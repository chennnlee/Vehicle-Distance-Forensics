# wow001 — WoWtchout 國道三號寶山段(白天,路人視角,遊覽車蓄意擋道)

素材:`data/input/wowtchout/wow_lb0uDMYUbaY.mp4`(YouTube WoWtchout 頻道,2026-07-25 10:39 拍攝,
2026-08-02 下載)。1920×1080 @30fps。**這是目前素材池中唯一同時具備「白天 + 國道 + 路人視角 +
事故/案件情境 + 多台可量測他車 + 118 秒連續原速」的一支**,補上了「汽車目標 + 路人視角」的素材缺口
(見上層 `dashcam_demo/README.md`「2026-08-02 新增兩支」)。

案件性質:前方遊覽車蓄意擋道/逼車,拍攝者為無關第三方。
(車牌與業者名稱已移除——量測用不到,而本文件會對外分享。)

> ⚠ 2026-09-18 狀態更新:**本資料夾的距離(`range_m`)、相對速率(`rel_ms`)與他車絕對速度,
> 是縱向尺度修正前的封存值**(2026-08-19 查出 SHARP 的固定 FOV 焦距假設使這台相機的距離偏高約 23%)。
> 引用 `range_m`、`rel_ms` 時乘 **0.815**;他車絕對速度**不能整個乘**——它等於自車速 + 相對速度×3.6,
> 只有相對速度那一項會縮。或直接用 `wow001_current/`(2026-09-16 現行碼重跑,已套係數、`ranges.csv` 有
> `far_field` 欄,**不要再乘**;目標跟車距離中位 22.5 → 18.0 m,見 `../_rerun/README.md`;
> 量法見 `../_calib/README.md`)。下文「仍然有效的部分」原本寫「可以直接用」,已一併更正。

## 影片結構(必讀:片長 124s,但只有 0–118s 是可用畫面)

| 片段 | OSD 時鐘 | 判讀 |
|---|---|---|
| 0–118s | 10:39:00 → 10:40:58 | **1:1 原速**(112 秒影片差對上 112 秒時鐘差),無變速無跳接 |
| 120s– | — | 片尾地圖/宣傳卡片 |

抽幀只取前 119s(3570 幀)。時基已用 OSD 時鐘全片驗證,**不是**時基陷阱案例。

## 執行參數(重跑用)

```
--frames-dir /tmp/wow001_frames --fps 30.0
--reference-pointcloud data/output/sharp_gaussians/wow001_ref.ply
--pointcloud-scale 0.962990 --scale-cv 0.0152 --hood-y 755
--odometer-points "1073,700;1116,720;1152,740;728,700;703,720"
--dash-cycle-m 10
```

- 參考幀 = f2101(t=70s,空路段);幀重建:`ffmpeg -t 119 -i <原片> -vsync 0 -qscale:v 2 /tmp/wow001_frames/f%04d.jpg`
- scale 錨定:`tools/dashcam_lane_width_calib.py`,車道寬 3.5m,3 組左右虛線像素對,
  CV **1.52%**,反推掛高 **1.219 m**(合理,自洽檢查通過)
- 自車在中間車道,左右**都是虛線**,故碼表點左右線各鋪
- 畫面下緣 CP2 TOUCH 廣告貼片(y≈845–1035)落在 hood 線以下,不影響量測

## ⚠ 自車速僅部分可用 —— 引用前務必看完本節

碼表覆蓋 74%(僅計碼表直接鎖定的幀;`../_rerun/README.md` 的 85.7% 另計入 ≤2s 內插與 1s 中位數濾波
補上的值,是同一份輸出的不同口徑),但對 OSD GPS 的 29 個檢查點:**MAE 11.97 km/h、24/29 鎖定、僅 5/24 落在 ±3 內**,
另有兩個 +56 km/h 的離群(t=96、108)。明細見 `ego_vs_gps.json`。

診斷過程(全部記錄下來,因為結論是「兩個問題糾纏、手上無第三方真值可拆」):

1. **週期 12m 已排除**:改用 12m 後 bias 從 −2.3 變成 +18.5,明確更差 → 國道三號同樣是 10m 級,
   與國1/台86 的 GPS 反驗結果一致(見 `docs/CASE_PARAMETERS.md` 的 dc006 / dc007 條目)。
2. **整數 lag 量化不是主因**:若把每個檢查點都取最接近真值的整數 lag,MAE 下限只有 1.97 km/h。
   我方 lag 在 18/24 個檢查點偏離最佳 lag ≥1 幀,部分達 2–3 幀。
3. **`_dual_line` 加倍閘不是元兇**:稽核 10910 筆候選,dual_line 觸發 **0 次**;
   93% 的候選 lag_selected == lag_argmax。問題出在自相關 argmax 本身。
4. **直接數脈衝(最不會騙人的做法)**:
   - t=64–88s(GPS 穩定 106):三個取樣點各自獨立數,脈衝間隔一致為 **11.0 幀**(IQR 10–11),
     24 秒共 64–68 個脈衝。週期 10m → 98.2 km/h。要讓 GPS 的 106 成立,週期須為 10.8–11.0m
     (非法定值)。**最省事的解釋是這台 abee 的 OSD GPS 高報約 5–8%,碼表反而是對的。**
   - t=8–12s:間隔 14–15 幀 → 72–77,與 GPS 95 差 28%,超出 GPS 偏差可解釋的範圍,是真的鎖錯
     (該段自車正急減速,踩到已知的 chirp 邊界)。
   - t=108–116s:脈衝間隔 IQR 3–35 幀,訊號本身就壞(兩個 +56 離群來自此段)。
5. 跨列時差法(同一條虛線先過遠列後過近列,不需週期假設)在時差小、無歧義的 t=56–76 段給出
   109–127 km/h,偏向支持 GPS;但該法本身踩到專案已記錄的失效模式(週期訊號 xcorr 多峰),
   不足以當仲裁。

**結論**:至少存在兩個獨立問題——(a) 這台 OSD GPS 疑似高報數個百分點,(b) 碼表在急減速段與
訊號劣化段真的鎖錯。沒有第三方真值(人工畫格法/外部 GPS)就無法拆開。

### 2026-08-02 補充仲裁:OSD GPS 不是金標準

用兩支**同時有 GPS OSD 與海盛人工畫格法真值**的國道片段做三方對照(摘要見上層 `dashcam_demo/README.md`
「連帶發現」,hs005 的明細見 `../hs005/README.md`):

| 片段(機種) | 速度帶 | 我方 vs 人工 | 該機 GPS vs 人工 |
|---|---|---|---|
| hs005(Garmin E530) | 89–97 | MAE **0.76** | MAE 2.09、bias −0.13 |
| hs006s(Garmin E350) | 92–98 | MAE **1.30** | MAE 2.91、bias **−2.89** |

**在有第三方人工真值的場合,我方碼表比機器自己的 GPS 更準,而且 GPS 偏差逐機種不同。**
所以本片「碼表 vs OSD 差 10%」不能直接判定成碼表錯。修正後的分段結論:

- **t≈56–92s(穩定巡航段):數字可用但標註不確定度。** 該段直接數脈衝三點一致 11.0 幀,
  演算法忠實反映了路面塗料,加上上表的仲裁結果,我方的 98–102 km/h 很可能比 abee OSD 的
  103–109 更接近真值。
- **t≈0–50s(跟遊覽車走走停停)與 t≈92–116s(訊號劣化):真的鎖錯,不可用。**
  前者踩 chirp 邊界,後者脈衝間隔 IQR 3–35 幀。

`abs_speed_median_kmh` 若要引用,**僅限軌跡落在 t≈56–92s 的目標車**,且需附上
「自車速未經人工真值驗證」的但書。

> ⚠ 2026-09-18 狀態更新:2026-09-16 的現行碼重跑(`wow001_current/`)多拒發了 215 幀(在封存裡
> 中位 132 km/h、範圍 72–138,其中 133 幀 >110 km/h,拒掉的主要是高速假鎖),
> 但仍有 161 幀 >110 km/h 未被拒(t≈95s 的 155 km/h 尖峰兩版都在)。
> 本片自車速依然未過驗證,見 `../_rerun/README.md`「wow001」節。

## ✅ 仍然有效的部分:距離與相對速度(⚠ 需乘距離修正係數 0.815)

`range_m`(跟車距離)與 `rel_ms`(相對速率)只依賴地面平面 + scale,**完全不經過碼表**,
而 scale 的錨定(車道寬 CV 1.5%、掛高 1.219m)是獨立自洽的——所以上面碼表的問題不影響它們。
~~這部分可以直接用。~~ **2026-09-18 更正:不能直接用。** 車道寬錨只約束橫向;前向尺度受 SHARP
固定焦距假設影響,本資料夾的封存距離偏高,引用時要乘 **0.815**(`rel_ms` 由距離微分而來,同樣要乘),
或直接用 `wow001_current/`(已套係數,不要再乘)。

`targets_summary.csv`:35 台目標車(近場 24、遠場粗估 9、無速 2)。
(2026-08-02 兩道追蹤器修正後重跑:關聯 px 一致性閘 37→35、縫合橫移預算閘 35→35;自車速逐幀不變。)
(2026-09-18 註:現行碼 `wow001_current/` 為 37 台,見 `../_rerun/README.md`。)

遊覽車(目標車)距離軌跡(修正前的封存值):全片在 10.7–26.6 m 之間反覆進退,最長單一軌跡 **id44** 涵蓋
t=4.5–34.4s(430 筆觀測、距離 10.7→26.6 m,由 6 個片段縫合而成)。

## 已知問題:遊覽車被切成多個 id

同一台遊覽車在 `targets_summary.csv` 中仍出現為多段(本資料夾封存輸出為 id34/44/79/128/162/186/271/311…)。
稽核後釐清(2026-08-02):**不是平行鬼影**——同幀高度重疊(交集/小框>0.65)的軌跡對是
**0 幀 / 2644 觀測**,現有同幀去重是好的。這些 id 在時間上**交替**(各自有大量空隙),
屬於斷軌後重生。真正的錯誤是**事後縫合跨接到別台車**:id113→id137 這筆,平面外插誤差只有
1.50 m(閘 6.00 m)卻在影像上位移 **479 px = 2.0 倍框寬**,把畫面左緣的另一台車接到遊覽車上。

縫合的閘目前**全在平面座標**(位置/速度方向/框高比/遠場),沒有影像空間檢查,而 6 m 的平面閘
在 16 m 深度處足以橫跨整個路面寬度 —— 洞在這裡。但**單一 px 門檻切不開**:跨三支 demo 統計,
被接受的合法縫合 disp_px/框寬 中位數 0.3–0.43、p90 約 1.5、最大到 6.65,這筆壞的是 2.00,
落在合法分布正中間。要修得靠「平面說幾乎沒動、影像說移了兩個車身」這種**跨空間矛盾**判準。
稽核工具已內建:`stitch_fragments(..., stitch_log=)`,或設 `DASHCAM_STITCH_LOG=<路徑>`
即可倒出每筆候選縫合的平面/影像指標。

**✅ 這筆縫合已於 2026-08-02 修好**:`stitch_fragments` 加了橫移預算閘 `|Δlat| ≤ 1.5×gap + 1.5 m`。
病因不是跨空間矛盾(平面與影像是雙射、同一份資訊),而是**平面誤差閘比的是等速外插**——
邊緣車抖動的 lat 讀值擬合出 3.6 m/s 假橫向速度,外插 1.4s 剛好落在遊覽車上,誤差只有 1.50m。
位移不像預測誤差那樣能被壞速度灌水,所以改用位移設限。t=25.33→26.73 那筆 4.76m 橫移
與另一筆 6.81m 都已消失;詳見上層 `dashcam_demo/README.md`「追蹤器修正之二」。

殘留:**關聯層**(gap ≤ 0.8s)本片仍有 1 筆超額橫移接點(t=34.13→34.70,2.42m/0.57s),
見上層 `dashcam_demo/README.md`「追蹤器修正之二」段末的「殘留」。

## 檔案

- `range_speed_h264.mp4` — 標注影片(107 MB;HUD 的 ego 速度僅 t≈56–92s 段可引用;
  距離為修正前,現行版影片在 `wow001_current/`)
- `original_clip_h264.mp4` — 前 119s 原片(可對照右下角 abee OSD 時鐘/GPS)
- `ego_speed.csv` / `ranges.csv` / `targets_summary.csv` / `tracks.json`
- `ego_vs_gps.json` — 29 檢查點對照(OSD 讀值為人工判讀,每 4 秒一點)

## 素材授權提醒

YouTube 公開影片,著作權屬 WoWtchout 與投稿者。作為研究/報告素材使用時須標註出處
(https://www.youtube.com/watch?v=lb0uDMYUbaY),不可作為商業用途。
