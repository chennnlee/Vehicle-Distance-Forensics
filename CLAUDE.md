# Vehicle-Distance-Forensics 專案指南

大專生研究計畫(交通事故影像鑑識):從監視器/行車紀錄器影片量測**車輛速度與距離**。
使用者是大學生(git: chennnlee),**回覆一律用繁體中文**;**commit 絕不加 Claude co-author**。
主要目標:汽車測速(機車次要)。報告示範素材用路口/道路 CCTV,不用騎樓商家鏡頭。

## 核心方法(兩條管線)

**幾何骨架(共用)**:挑一張參考幀跑 SHARP(`sharp predict -i img -o dir --device cpu --no-render`,
CPU-only,GPU 6GB 會 OOM,約 90 秒)→ RANSAC 擬合地面平面 y=ax+bz+c(`lane_dash_calibration.fit_ground_plane_raw`)
→ 之後所有像素都用「射線×平面求交×scale」轉成路面公尺座標。SHARP 只跑一次,不逐幀。
**SHARP 是固定 FOV 假設(fx≈0.7955×寬)**,每台裝置 scale 不同(0.88~2.9),必須用法定錨校正。

**尺度錨(台灣法定標線)**:
- CCTV(高視角):`tools/lane_dash_calibration.py` 量虛線 3D 長度。規格:車道線 4m/6m、高速公路 4m/8m、
  路口導引線 0.5m/0.5m(gap/dash 比先做免尺度規格識別)。多鏈剝離=每條虛線一把尺(跨鏈互驗)。
- 行車紀錄器(貼地視角):**虛線 3D 取樣失效**(切線視角深度雜訊,4m 量成 0.3~6.7m)。
  改用:車道寬(國道 3.5m)、相機高度轉移(同鏡頭同車跨片段)、或 GPS。
  **車道寬錨定已正式化為 `tools/dashcam_lane_width_calib.py`(2026-08-02)**——在此之前
  dc003/006/007 是臨時腳本、隨 /tmp 消失。餵左右車道線像素對,輸出 scale + CV,
  並**反推相機掛高當獨立自洽檢查**(合理 1.1~1.5m;算出 0.6 或 2.5m 就是錯了,即使
  車道寬殘差看起來很緊)。虛線像素別目視挑:多幀局部對比累積會把間斷虛線抹成連續脊線,
  逐列取峰即得(hs005 CV 0.66%/掛高 1.229m、wow001 CV 1.52%/1.219m)。

**管線 A:固定 CCTV 測速** `tools/cctv_speed_estimation.py`
NVR 串流謊報 fps → OSD 時鐘秒跳定時(無 OSD 用 --fps)。YOLOv8m-seg 接地點(mask 最低 12%)→
平面座標 → 匈牙利+方向閘+bbox尺寸閘追蹤 → 等速擬合。品質防護:淨位移門檻(防停等抖動灌水)、
遠場旗標(靈敏度 m/px>0.3 拒發速度)、±95%CI(擬合標準誤⊕scale CV;CI>50%速度→灰色問號)。
**v3 追蹤器(防框跳動/重複計數,2026-07-10)**:同幀重複框抑制(car+truck 雙框同車,交集/小框>0.65
留高信心)、類別群組關聯(car/truck/bus 同群,防類別閃爍剪軌;機車獨立群;回報=多數決)、
斷軌事後縫合(`stitch_fragments`:等速外插+尺寸+方向閘,`merged_from` 稽核)、平行鬼影軌抑制、
遠場防護鏈(遠場觀測不進速度擬合/不當縫合端點/關聯閘鎖 4m——防「地平線漫遊」把對向車接進來)、
關聯閘物理化(速度誤差項+4m/s² 加速度項;舊 speed×dt×1.6 斷檔數秒後會吞下一台車捏造 ~100km/h)、
渲染顯示框 (¼,½,¼) 置中平滑(量測資料不動,時間斷點不平滑)。
影片:--anchors-json 畫校正線、HUD、--also-frame-video 逐幀版、自動 h264。

**管線 B:行車紀錄器** `tools/dashcam_range_speed.py`
移動相機,但地面平面「相對相機」不變(平路+車高固定)。
- **自車速=虛線週期碼表**(唯一信任來源):法定週期流過固定列,滑動自相關取週期。
  免 scale、免畸變、免震動。**週期勿信規範先驗**:國1 五股段/台86 實測都是 10m(非 12m)——
  有 GPS OSD 就先反驗(脈衝間隔幀數=cycle/v×fps,10 vs 12 差 20% 可辨)。
  防護鏈(每道都是實際失效換來的):訊號 std>2(停止時假鎖)、
  窗內≥2 完整週期(爬行假峰)、雙窗 3s/5.1s 一致 12%(八度錯)、**峰突出度≥0.3**(突發訊號寬肩 0.03 vs 真鎖 0.7+)、
  跨列相位(引擎震動全域同相,真虛線先遠列後近列)、強鎖免雙窗(conf≥0.5 且 prom≥0.5,救短片減速段)。
  **2026-07-11 新增四道(白天三支 demo 實際失效換來)**:`--octave-threshold`(預設 0.75 不動舊案;
  乾淨巡航時有限窗自相關衰減 ac[2L]/ac[L]≈(W−2L)/(W−L)≈0.85>0.75 會系統性翻半速,
  帶內幾何上無第二條線時用 0.95)、線位穩定閘(窗內脈衝 x-IQR>50px 拒發=換道掃過匝道密集短線,
  82 真速讀 146 案例)、sub-octave 修正(國道貓眼每隔一根 dash=20m 子結構讓 argmax 直接選 2L 半速;
  lag/2 有局部峰且 ≥0.6×ac[lag] 就取回)、分裂鎖閘(cand 分歧 >1.5× 時兩點「中位」=平均出幻速,
  改取多數群否則拒發)。跨列相位「速度」閘試過拆掉(週期訊號 xcorr 多峰不可辨,會誤殺好段)。
  **✅ 2026-07-24 八度邏輯已重構為一次性基頻選擇**(取代上面「八度防護+sub-octave」加倍/減半鏈——
  稽核發現該鏈在五 demo 上 98–100% 先加倍再撤銷=靠兩 bug 抵銷,thr 0.75/0.95 輸出中位差 0.00 死碼,
  且「鄰道虛線+真速≲77km/h」合成情境會漏出高信心雙倍速 54→108)。新法:對候選
  {argmax/2, argmax, 2·argmax} 做**諧波梳狀評分**(`_comb_score`=comb−anti;真基頻整數倍是峰、
  半整數倍是谷分數高,諧波/子諧波的半整數倍落在真峰上分數低)取最高;其上疊 **pulse x-offset 雙線閘**
  (`_dual_line`:自相關對亮度分不出「一線走半週期 vs 兩交錯線走全週期」,靠脈衝在兩橫向位置交替判定,
  真週期=**2×實測脈衝間距**,非 2×argmax——高速時 argmax 已在每線週期,加倍會過修)。
  迴歸:五 demo archive_diff 逐位不變、逐幀 median|Δ|=0.00(中位 59.6/100.0/96.3/83.4/55.0);
  合成 neighbor 54→54.0、43.2→43.3、72→72.1,clean/studs 不變。`--octave-threshold` 旗標保留只為相容已失效。
  副產品:dc008 封存輸出 t≈18.4s 的 28.4km/h 是舊碼半速錯(GPS=57),現行碼已自動修好(archive_diff p95 28.45)。
  **✅ 2026-08-17 在梳狀評分之上再加「跨時間聯合八度選擇」`_join_octaves`(現行預設,`--no-join-octaves` 關)**:
  梳狀評分是**單窗內**的決策,而夜間逆反射標記讓半週期成為真峰、五個形狀特徵都分不開 → 單窗無解。
  改法:每個取樣點跑 Viterbi,發射項=梳狀分數(相對該幀最佳,故各幀等權),轉移項=對隱含速度變化收費,
  候選只取該幀自相關真有的峰(`_peak_snap`),`_dual_line` 命中時該幀只有一個候選(直接觀測不該被覆寫)。
  **轉移項務必用 |Δ log v| 而非 |Δv|**——低一個八度的路徑速度變化也低一個八度、永遠更平滑,
  罰絕對值等於獎勵減半(第一版把 dc003 覆蓋打成 27.1%、wow001 中位打成 77.9)。
  一次性切換代價約 20×(log2−tol),持續 ≥3s 的真實八度改變仍會跟上且切換點落在正確邊界幀;
  斷訊處斷鏈不傳遞。成效與迴歸見驗證表 comma2k19 列。
  事件統計 schema 更新為 changed/period_up/period_down/dual_line(`dash_cycle_speeds(..., lag_events=)`,純觀測),
  稽核工具 `tools/odometer_lag_audit.py`;報告見 `data/output/odometer_lag_audit/README.md`。
- **追蹤器 v3 移植(2026-07-16,已 commit 8c2e992)**:CCTV v3 的類別群組關聯/同幀重複框抑制/
  斷軌縫合(2D 相對平面版,gap≤2s、遠場縫合禁止)/平行鬼影抑制/多數決類別已移植進管線 B;
  targets_summary.csv 由管線正式輸出(近場中位速 + 遠場粗估分欄,遠場不再混入主數字)。
  **五支 demo 全部重跑在 `dashcam_demo/*_trackv3/`(2026-07-18)**,近場速度與封存一致:
  dc006 89 目標=29 近場+45 遠場粗估+15 無速;dc007 紅車三段誠實分段(貼身空窗正確拒縫);
  dc008 半速段修復實證(t18.4s 舊 28.4→新 56.8,GPS OSD=57,舊版連他車速度都被拉低一半);
  dc002 油罐車 id3 單軌 0–12.6s(62.7 km/h vs 自車 GPS 58–59,舊版 10.5s 斷軌後誤判 car);
  dc003 62 目標 55 台近場有速,最長單軌 18.8s。新舊逐幀對照圖做過(dc008/dc002),
  差異在時間軸記帳與數字誠實性,單幀畫面本來就該一致(迴歸安全)。
- 光流**不給任何速度值**(震動/夜間會把 60km/h 讀成 2);停止=路面帶幀差變化比例<0.02 且碼表訊號活動度<3。
- 他車:接地點→平面→跟車距離;絕對速=自車速+d(range)/dt;無自車速時只給距離。
- 無讀值誠實標 no lock,只內插 ≤2s 空隙。

## ⚠ OSD GPS 不是金標準(2026-08-02 仲裁,會改變既有驗證數字的解讀)

拿**同時有 GPS OSD 與海盛人工畫格法真值**的兩支片做三方對照,結論是**我方碼表比機器自己的
GPS 更接近人工真值**,而且 GPS 偏差**逐機種不同**:

| 片段(機種) | 速度帶 | 我方 vs 人工 | 該機 GPS vs 人工 |
|---|---|---|---|
| hs005(Garmin GDR E530) | 89–97 | MAE **0.76**、bias −0.04 | MAE 2.09、bias −0.13(無偏但散) |
| hs006s(Garmin GDR E350) | 92–98 | MAE **1.30**、bias +0.19 | MAE 2.91、bias **−2.89**(系統性低報) |

推論:**過去所有「vs GPS OSD」的驗證(dc002 MAE 1.2、dc006 2.1、dc007 2.2、dc003 ±4、
20251029 批量)有一部分誤差其實是 GPS 裝置誤差,我方實際精度應優於已記錄數字。**
有人工畫格法真值時一律以人工為準;只有 OSD 時要註明「對照對象本身有 ±3 km/h 級不確定度」。
這也解釋了 wow001 對 abee OSD 的落差有一部分不是我方的錯(但該片仍有真失效段,見其 README)。

**碼表迴歸框架(2026-08-02 建立,2026-08-17 案表改為 durable)**:15 案(五 demo + 20251029 六案
+ hs005/wow001 + 仲裁 hs003s/hs006s)一次跑完,訊號抽取快取在 `/tmp/odo_sig_cache/`。
**案表(來源片路徑 / fps / 取樣點 / 抽幀窗 / 人工真值 xlsx)已進版控:`tools/odometer_regression_cases.py`**
——2026-08-17 重建,因為原本放 scratchpad 的那份隨 /tmp 消失過一次;15 案素材路徑當日全部驗證存在。
hs003s/hs006s 的取樣點從未記錄過,案表標 `points=None` 表示改用 `auto_odometer_points.py` 挑
(before/after 用同一組點,迴歸照樣成立)。評分器/幀重建器仍是 scratchpad 拋棄式
(`rebuild_frames.py` / `manual_truth.py` / `odo_regress.py`)。
人工真值可**機器讀取**(`*_manual.xlsx` 的 基準畫格起/終 + KM/PH 列),不必逐幀目視讀 OSD。
基準線(before):七個有人工真值的案 ALL MAE 6.33 / bias −2.98(含失敗的 hs003s);
排除 hs003s 後 c0135 2.45、c1005 2.59、c3323 2.69、c4316 2.03、hs005 0.76、hs006s 1.30。
hs003s(Mio 超廣角)覆蓋僅 31%、MAE 25.3 = 取樣點落在**連續縱向亮痕**上而非虛線,
與 dc003 同機同病,要用 CLAUDE.md 的「駐留平台」法重挑點才可用。

## 🔴 行車紀錄器距離偏高 11–32%:碼表可以自己校掉(2026-08-19 定案,同日晚間全案重跑核對)

**結論:SHARP 的平面俯仰大致正確(獨立證據=車道線消失點),錯的是它的固定 FOV
焦距假設,而且逐機不同。**
影響**距離與他車絕對速度**;**自車速完全不受影響**(碼表不經過 SHARP)。

**怎麼量的(不用平面、不用 SHARP、不用車道寬、不需要右車道線)**:同一批虛線先後流過
各取樣列,兩列訊號的**互相關 lag ÷ 碼表週期 × 法定週期 = 兩列的實際公尺間距**。
再把 `fwd(y) = A/(y − y_h)` 擬合上去,`A = h·f` 就是距離量測唯一需要的量
(h 與 f 不必分開解)。

hs005 兩參數擬合(取樣列跨 150 px,條件好):
| | A = h·f | 地平線列 | 殘差 |
|---|---|---|---|
| 碼表實測 | **1364** | 599.1 | 0.083 m |
| SHARP | 1891 | 595.4 | — |

差的**幾乎全在 A**,且每一組列對的比值都是均勻的 1.31 倍
——**固定比例正是焦距誤差的特徵,俯仰誤差會隨距離變化**。

⚠ **兩參數擬合出的地平線不穩**(A 與 y_h 強相關):hs005 兩次跑分別得 596.6 與 599.1
(即與 SHARP 差 0.7 px 或 3.7 px)。**俯仰的獨立證據要用車道線消失點(hs005 實測 600.5),
不要引用擬合出的地平線**;先前寫的「地平線只差 0.7 px → 俯仰正確」是那次擬合的偶然值。
而且**係數對地平線的選擇不敏感、方向也不會翻**:hs005 用 SHARP 的 595.4 得 0.760、
用擬合的 599.1 得 0.719、用消失點實測的 600.5 得 0.707 —— 把地平線移到消失點實測值
**反而讓預測距離更大、偏差更嚴重**,所以偏差不可能是俯仰造成的。

**逐機修正係數**(工具 `tools/odometer_distance_calib.py`;圖 `data/output/dashcam_demo/_calib/effect.png`)
——**下表是 2026-08-19 晚間用版控參數 + 由 `original_clip_h264.mp4` 重建影格全案重跑的結果,
這組才可重現**(舊表那組已作廢,差異見其下):

| 案 | 列對 | 修正係數 | 逐對散布 | SHARP 偏高 | 判定 |
|---|---|---|---|---|---|
| hs005 | 6 | **0.760** | 0.042 | +32% | ✓ |
| dc002 | 3 | **0.811** | 0.005 | +23% | ✓(2026-09-16 由 hood-y 更正,見下)|
| wow001 | 4 | **0.815** | 0.016 | +23% | ✓(新通過)|
| dc008 | 3 | **0.854** | 0.037 | +17% | ✓(2026-09-16 由 hood-y 更正,見下)|
| dc006 | 3 | **0.903** | 0.035 | +11% | ✓ |
| dc003 | 2 | — | 0.985 | — | ✗(兩對 1.378/0.394,與該片已知超廣角失效一致)|
| dc007 | 1 | — | — | — | ✗ 列對不足(取樣點僅 3、片長 11s)|

⚠ **與當日稍早那組記錄的差異**(必須記著,因為對照影片是用舊值算的):
- **wow001 由「拒發」變「通過」**:舊記錄的散布 0.134 是**加「同線分組閘」之前**的數字,
  該片有兩點在對側車道線;加閘後散布 0.016。→ **7 支 demo 有 5 支可自校**,不是 4 支。
- dc006 0.923→**0.903**、dc002 0.845→**0.831**、dc008 0.874→**0.886**
  (舊表與交接段本身就互相矛盾,現已統一)。差異來源是重建影格後互相關峰的次幀位置略移,
  幅度 ≤2%,遠小於本量測自己的逐對散布。
- **對照影片 `_calib/*_before_after.mp4` 用的是舊值**(hs005 0.760 相同、dc002 0.831、
  dc008 0.886、dc006 0.923),差 ≤2%、肉眼不可辨,不重算。
  ⚠ 2026-09-16 更正後差距變成 dc002 2.4%、dc008 3.6%(仍肉眼不可辨,仍不重算;
  但引用影片裡的數字時要知道它用的是舊係數)。

🔴 **2026-09-16 更正:dc002 與 dc008 的係數是用「猜錯的 hood-y」算出來的。**
封存的 hood-y 從未記錄,8/19 是由畫面目視讀成 850 / 950;但 hood-y 是**平面擬合 ROI 的下緣**,
猜錯就擬到不同的平面,係數與距離全部跟著錯(dc008 隱含地平線差 9.5 px、距離差約 6%)。
**不必用猜的**:`tracks.json` 存了該次執行用的平面係數,而平面擬合在「同點雲 + 同 hood-y」下是
確定性的,所以掃 hood-y 就能反解——兩支都 **EXACT 命中 hood-y = 900**
(工具 `tools/solve_archived_hood.py`)。用 900 重算:

| 案 | 舊(猜的 hood) | 新(反解的 hood=900) | 逐對散布 |
|---|---|---|---|
| dc002 | 0.831(hood 850) | **0.811** | 0.0138 → **0.0054** |
| dc008 | 0.886(hood 950) | **0.854** | 0.0475 → **0.0366** |

**兩支的逐對散布同時變小**,這是「900 才對」的獨立佐證(幾何更自洽)——不是只有係數變了而已。
⚠ 舊記錄寫「係數對 hood-y 不敏感,差 1.6%」是**低估**:實際差 2.4%(dc002)與 3.6%(dc008),
且 dc002 的變動遠大於它自己 0.005 的散布。其餘五支的 hood-y 都有記錄,平面經比對**逐位元相同**
(dc003/dc006/dc007/hs005/wow001),係數不受影響。

**五支通過的片全部同方向**,SHARP 距離偏高 11–32%,逐機不同。
效果(目標跟車距離中位)——**2026-09-16 起是端到端重跑的實測值,不再是「封存中位 × 係數」的預測**
(輸出在 `dashcam_demo/*_current/`):hs005 34.1→**26.0** m、dc006 43.4→**39.1**、
dc008 34.3→**32.8**、wow001 22.5→**18.0**、dc002 18.5→**15.3**。
⚠ 實測與「中位 × 係數」不會完全相等,因為距離縮放會改變追蹤器的關聯/縫合與遠場判定,
目標的切分跟著變(dc008 目標 18→15,中位由不同觀測組成,所以 32.8 高於預測的 29.3)。
幾何本身是對的:每支的 `A = fwd²/sens` 比值都等於其係數(dc008 0.851 對 0.854)。
圖由 `tools/report/plot_distance_calib.py` 重畫
(讀 `_calib/*.json`,不必重跑量測;被拒發的 dc003 也畫進去,一點在線上方、一點遠在下方)。

**為什麼確定是「尺度錯」而不是「互相關峰被拉偏」** —— 這條推翻 8/18 自己下的判決。
⚠ 先記一個**我自己犯過的推論錯誤**:一開始我用「乘性 vs 加性」模型比殘差(見下表),
但那個檢定是**錯誤設定**的 —— 加性假設寫成「所有列對共用一個常數 δ」,而物理上該質疑的
對立假設是「**每一列各有自己的位置偏差 b_i**」,那個模型有跟列數一樣多的自由度,
把資料擬得跟乘性一樣好(hs005 四列實測:b 差值互相自洽到 0.06 m)。**兩個模型分不開。**

**真正的判別做完了(2026-08-19 晚間),而且結論相同,理由更硬**:
1. **解析計算**:被質疑的機制是「13 列取樣窗在遠列覆蓋 2.3 m 路面、近列只有 0.36 m,
   造成脈衝不對稱把互相關峰拉偏」。但窗在**影像列**上對稱,轉成公尺後的不對稱量是
   `½(fwd(y−6)+fwd(y+6)) − fwd(y)`:hs005 遠列(700)只有 **+0.060 m(0.33%)**、
   近列(850)**+0.004 m(0.06%)**。對 4.47 m 的列對間距只影響 **1.2%**,解釋不了 32%。
2. **實測**:把取樣窗高度從 **1 列掃到 21 列**(±0→±10),修正係數只在
   **0.771 → 0.758** 之間動(±1.5%)。若脈衝寬度是病因,係數必須隨窗高大幅改變 —— 它沒有。
   (副作用:窄窗的逐對散布較大 0.18,寬窗 0.015,所以預設 13 列是合理的。)

**🔴 2026-08-20 新增:用「已知尺寸的物體」當第三方尺,結果與 0.760 不一致 —— 幅度未定案。**
這是計畫書原本就列的錨(車型軸距 / 漆線長 / 車牌),但一直沒用。手動點座標的快速探測(hs005 f270/f330):

| 探測 | 真值 | 未修正量到 | 隱含係數 | 條件 |
|---|---|---|---|---|
| Lexus LS 430 軸距(車牌 8726-WK,f270) | 2.925 m | **3.15 m** | 0.93 | 兩輪接地點在 9.2 / 12.4 m,±5 px → ±0.18 m |
| 同車 f330 | 2.925 m | 6.18 m | — | ❌ 作廢:前輪在裁切邊緣、遠場敏感度 0.14 m/px |
| 近場漆線全長(f330,目視兩端) | 4.0 m | **5.60 m** | 0.71 | 兩端在 5.9 / 11.5 m,近場條件好 |
| 同一量但用門檻式自動偵測 | 4.0 m | 4.52 m | 0.89 | 半高門檻會把模糊的漆線邊緣切掉 |

→ **同一支片、同一個問題,四個數字給 0.71 / 0.89 / 0.93 / 1.32(跨列法)。**
單次目視點座標解決不了(±5 px 就 ±6%,而漆線「邊緣算到哪」本身就有 ±20% 的定義問題)。
~~**在做完多樣本版本之前,距離修正係數應標為「初步、方向確定、幅度未定案」。**~~
✅ **2026-09-01 由 comma2k19 原廠雷達結掉**:跨列碼表法推的係數與雷達一致到 1.6–3.6%
(見上方「他車距離與速度已對外部儀器驗證」段)。這裡這些手動點座標探測**散布大是它們自己的問題**
(單次目視點 ±5 px、漆線邊緣定義 ±20%),不是跨列法的問題。
⚠ 車牌寬度**代數上無法**回答這題:apparent px = W·(y−y_h)/h,焦距會消掉 → 它只驗掛高與俯角,不驗前向尺度。
要驗前向尺度,錨必須是**沿行進方向**的長度(軸距、漆線長、虛線週期)。

**旁證與其偏誤(記著,免得下次又被自己的快速探測嚇到)**:
- **路面圖樣退行速度 vs GPS**(用 GPS 當速度基準,完全不經過虛線週期假設):
  hs005 得 **1.237**、dc002 **1.123** —— 方向相同、幅度略小於跨列法的 1.316 / 1.203。
  該估計量**已知偏低**:公尺空間中的圖樣近處銳利、遠處糊,互相關峰被拉向近處 → 位移低估。
  而且它對距離窗很敏感(hs005 分窗 7–15/12–22/20–32/28–42 m 得 1.04/1.13/0.99/1.12,
  窗長不足一個週期時根本不可信)。**當下界看,不當主值。**
- **單幀空間週期(量虛線間距)不可用**:五支同型道路量到 8.7–11.1 m(真值都是 10),
  ±15% 雜訊 —— 畫面裡只裝得下 2~3 個週期,低於本專案自己的「窗內 ≥2 週期」判準。
  ⚠ 而且**比較修正前後時必須用同一組像素**:若用「公尺區間」選樣本,前後選到的其實是
  不同的路面片段,前後差異變成選樣造成的(我踩過:dc008 前 9.51 / 後 9.54,比值 1.003,
  但純幾何縮放必須恰好等於係數 0.886——該案的係數已於 2026-09-16 更正為 0.854,
  此處保留原數字因為要講的是「選樣造成的假一致」這個方法論錯誤,不是係數值本身)。

原本那個(設定錯誤但結論相同的)乘性/加性對照留在下面備查:

| 案 | 列對間距跨幅 | 乘性 k / 最大殘差 | 加性 δ / 最大殘差 |
|---|---|---|---|
| hs005 | 5.9× | 0.760 / **0.066 m** | 1.30 m / **1.300 m** |
| dc002 | 2.5× | 0.830 / 0.011 m | 0.33 m / 0.240 m |
| dc008 | 3.5× | 0.886 / 0.030 m | 0.15 m / 0.100 m |
| dc006 | 2.2× | 0.904 / 0.063 m | 0.33 m / 0.220 m |
| wow001 | 2.3× | 0.812 / 0.032 m | 0.43 m / 0.355 m |

hs005 的列對間距從 1.8 m 到 10.7 m(5.9 倍),赤字始終是同一個**比例**、不是同一個**公尺數**。
→ **8/18 寫的「跨列時差法有已指認的偏誤(脈衝寬度不對稱+週期重疊把峰往小 lag 拉),不要再用」
這條判決撤銷**:那個探測量到的 0.77 一直是對的,錯的是當時的**解釋**——
當時以為唯一的替代解釋是俯仰誤差,而消失點已證俯仰沒問題,於是反推探測本身有偏。
真正的第三個選項是**焦距**,它同時滿足「俯仰正確」與「距離差 30%」。

**兩道品質閘都是實際踩到才加的**:
- **只配對同一條車道線上的取樣點**。左右虛線縱向相位不同,跨線配對的 lag 不是行進時間。
  dc003 不分組時解出 5.209 的荒謬係數;dc006 由 0.856 變 0.90 級(對側點 1319,820 被排除);
  wow001 更是由「拒發」變「通過」。
- **修正係數用「實測/預測」的直接中位數,不用擬合 A 的比值**。A 與 y_h 在 3–6 組列對下拆不穩:
  dc006 的兩參數擬合把差異全算進地平線而報出「不用修」,但每一組列對的實測都比預測低約 10%。
  直接比值不需要做這個分解,而**片內一致性本身就是地平線大致正確的證據**
  (地平線若錯,比值會隨列對系統性變化)—— 所以散布 >0.10 就拒發。

⚠ 取樣列跨幅太短時兩參數擬合會退化(wow001 只跨 40 px、dc008 只跨 60 px),工具會自動改用
固定 y_h 只解 A。此時逐對散布的檢定力也較弱(所有列對幾何相近),wow001 的 0.815 應視為
「可用但條件不如 hs005」。

**與模型內參的對照(回應計畫書階段一)**:UniDepth v2 對 hs005 預測 fx=843
(等於修正係數 0.53),**比實測的 0.76 還過頭**;SHARP 假設 1527(係數 1.0)則偏另一邊。
逐機預測:dc003 882 / dc006 878 / dc007 1507 / hs005 843 / wow001 748。
→ **兩個模型的內參都不可信,物理量測才可信** —— 這正好是計畫書「雙軌校正」的精神,
只是可信的那一軌是標線與碼表,不是模型解耦。

**修法很便宜**:修正係數是**一個乘數**,直接乘在既有距離輸出上,不必重建幾何、
不必重跑 SHARP。而且**每支能跑碼表的片都能自己校**(碼表本來就要跑),不需要額外素材。

## ✅ 他車距離與速度已對外部儀器驗證過了(2026-09-01,comma2k19 原廠雷達)

**這是本專案第一次用儀器驗證「他車」的數字。** 之前發表過的跟車距離與目標絕對速度
從來沒有對過任何外部真值,唯一的自我檢查「穩定跟車時 |abs−ego| 很小」**在代數上是循環的**
(`abs=(ego+rel)×3.6` ⇒ `|abs−ego|≡|rel|×3.6`,篩穩定跟車就是篩 |rel| 小)——**那個 0.10 不是精度**。

**破解循環的東西**:comma2k19 每段都帶該車的 **DSU 雷達**(`processed_log/CAN/radar`),
給前方物體的縱向/橫向距離與相對速度;配上 CAN 輪速就有「目標絕對速 = CAN + 雷達相對速」。
與我方完全不共用:不同感測器、不同物理、不同自車速來源。
欄位無文件但**自己驗證自己**:靜止路側物體的相對速正好等於 −CAN 車速。
工具 `tools/comma2k19_radar_export.py` + `tools/comma2k19_radar_eval.py`;
報告與全部參數見 `data/output/comma2k19_radar_eval/README.md`。

**結果(3 段 / 908 配對幀,分層用管線自己的靈敏度旗標 sens=fwd²/A)**:

| 分層 | n | 雷達/我方 中位 | 距離誤差中位 | 速度 bias | 速度 MAE | p90 |
|---|---|---|---|---|---|---|
| 全部 | 908 | 0.994 | 9.6% | −1.62 | 5.13 | 7.75 |
| **近場 sens≤0.8**(≲32 m) | 434 | **0.946** | **7.0%** | **+0.39** | **3.54** | 6.18 |
| 遠場 sens>0.8 | 474 | 1.156 | 20.5% | −3.39 | 6.53 | 11.86 |

**誤差可精確拆解**(恆等式,實測相加誤差 <0.11 km/h):
`abs_ours − abs_radar = (ego_碼表 − ego_CAN) + 3.6×(rel_ours − rel_radar)`。
近場:自車速項 MAE 1.94、**幾何項 3.28**(兩者同量級);只換自車速為 CAN 得 3.28 / bias −0.33。

**平均確實有效,但改善要拆成兩半**(只在同一組「我方軌↔雷達物體」的連續段內平均——
跨段平均會把兩台不同的車平均在一起,第一版就這樣把 seg21 的 3s p90 從 14.63 弄壞成 28.76):
瞬時 5.13 → 1s 3.99 → 2s 1.90 → **3s 1.44**。其中**平均本身只佔一半**
(同一批幀的瞬時是 2.90 → 1.44,比值 0.50),另一半是**選樣**(撐得過 3s 窗的是長而穩定的配對)。

**沒有系統性時間延遲**:±2s 掃描的最佳延遲三段分別是 **+1.95 / −1.60 / +2.00 s**,
正負號不一致且兩個撞到搜尋邊界 → 那是「把兩條緩慢漂移的誤差對齊」的過擬合,**不要做延遲補償**。

🔴 **要修的(範圍比初判小,2026-09-16 讀碼更正)**:**HUD 與 targets_summary.csv 其實都是對的**
——`dashcam_range_speed.py:1169` 判 `far = sens > 0.8` 後,HUD 只印灰色的 `id{n} {cls} far-range`,
**距離與速度都不顯示**;摘要檔也把遠場速度分到 `abs_speed_median_far_kmh` 獨立欄。
**唯一沒處理的是 `ranges.csv`**:它逐筆寫 `range_m`,只附原始的 `sens_m_per_px` 欄,沒有旗標也沒有說明,
而那個檔正是給人拿去做後續分析的介面。遠場距離中位偏 20%、最壞偏 **72%**
(seg21 遠場比值 1.719:我方 38 m、雷達 66 m)。
✅ **2026-09-16 已修**:`ranges.csv` 新增 `far_field` 欄(0/1,判準與 HUD 相同),既有欄位不動。
封存輸出沒有該欄,自行判定用 `sens_m_per_px > 0.8`(⚠ 該欄只印三位小數,門檻上的觀測會印成 0.800
而實際大於 0.8,極少數邊界筆會不一致;旗標本身用未捨入值)。

**✅ 順便把「距離修正係數幅度未定案」結掉了**(取代 2026-08-20 那條):

| 段 | 碼表跨列法係數 | 逐對散布 | 雷達近場實測比值 | 差 |
|---|---|---|---|---|
| seg21 | 1.031 | 0.053 | 0.995 | **+3.6%** |
| seg6 | 0.919 | 0.083 | 0.934 | **−1.6%** |
| seg10 | (1.028) | 0.113 → **品質閘拒發** | (無近場觀測) | — |

碼表推的係數與雷達一致到 **1.6–3.6%**,都落在它自己的逐對散布內;**品質閘也正確拒發了 seg10**。
⚠ **驗證的是方法,不是台灣那五個數字**——這台相機需要的修正接近 1.0(0.92–1.03),
沒有測試到「0.76 這種大修正」的情形;它證明的是這個量法不會系統性騙人,精度約 ±4%。

**SHARP 的地平線再次被獨立拒絕**(seg21,唯一距離跨幅夠大的一段 7.4–68.7 m):
用雷達距離 + 接地點像素列反解 `d_radar+Δ = A/(py−y_h)`(Δ=雷達在保險桿、相機在擋風玻璃,
當待解參數;殘差取在像素上)→ 自由擬合殘差 **2.60 px**;**強迫用 SHARP 的 y_h 要付 +5.06 px
並需要物理上不可能的 +5 m 安裝偏移**。對照 y_h:車道線消失點 377.8 / 碼表 374.3 / SHARP 365.2。
⚠ **別把「A 差 1.36 倍」講成「距離差 36%」**:A 與 y_h 在有限跨幅下強烈交換,兩組參數近場幾乎重合
(實測近場比值就是 0.995),可引用的是**分距離帶的實測比值**,不是分解出來的 A。

**台灣端零速錨:掃過但不可用(否定結果)。** 全部 14 個封存輸出中**沒有任何停止的目標車**
(掃到的單調接近軌全是高速公路上略慢的前車,closing/ego 只有 0.09–0.16;城市案也沒有)。
dc008 兩個候選都不行:id110 橫向從 +7.8 漂到 −16.7 是路口橫向穿越車;id250 是路邊停放廂型車,
但**接近過程距離先變大**(32.8→34.2)才驟降 —— **擦身而過時接地點會沿車身遷移**(車尾→車側),
量到的接近率只有自車速的 −0.02 倍(應為 1.00)。
→ 得到一條可用邊界:**橫向偏移大、距離近的擦身目標速度不可信**(是系統性遷移,不是雜訊)。

**⚠ 順手修掉的一個潛在 bug**:`odometer_distance_calib.py` 原本**寫死 1920×1080 的內參**
(`build_plane(..., (1080,1920))`、`y_h = 539.5 + 1527.4·b`),換一台感測器就把地平線算錯幾十像素
(comma2k19 是 1164×874)。已改成由影格讀尺寸、由點雲帶回內參(`build_plane` 多回傳 intrinsics)。
**對台灣既有係數的影響已量過**:地平線一律移 +0.50 px(舊碼投影用 cy=540.0 卻拿 539.5 算地平線,
本身不自洽),係數變動 **−0.17%~−0.44%**,遠小於各案自己的逐對散布 1.3–4.7%;
舊路徑能 1e-6 重現每一個已發表係數。**發表的數字不需要改。**

## 🟡(已被上一段取代)舊版:縱向度量上界 ~9%(2026-08-18 提出,2026-08-19 收緊)

有人提出「SHARP 訓練基線只有 60cm,遠場度量不可信」。查證的結論分成兩半:

**不受影響的那半(已用程式碼確認)**:自車速完全不經過 SHARP。
`public_dataset_ego_eval.py` 只 import `pick_points / dash_cycle_speeds /
extract_odometer_signals`,碼表函式內沒有任何 plane / ply / scale 參照。
→ comma2k19 兩台車 2.18、KITTI 邊界、夜間修復、15 案迴歸**全部與 SHARP 無關**。
而且 SHARP 的**逐像素深度從不進入量測**:`pixel_to_plane` 是解析射線×平面交點
(`t = c/(dy−a·dx−b)`),SHARP 只貢獻三個平面係數 (a,b,c),由近場 ROI 擬合。
「度量深度不可信」這個前提,本架構本來就假設了。

**但查證過程翻出一個更嚴重、且**先前完全沒被檢驗過**的問題:縱向度量。**
尺度錨是**車道寬**——橫向量的。它錨定 scale,卻對「縱向距離是否正確」幾乎沒有約束力。
在 hs005 上做了三個獨立探測,方向一致地指出**平面高估縱向距離**:

| 探測 | 結果 | 可信度 |
|---|---|---|
| 漆線長(法定 4 m,單幀、n=107) | 4.70 m,**+17.6%** | 中(門檻式偵測,k 變 5% 輸出跳 18%,±5–10% 不穩) |
| 同上,較少影格(n=41) | 4.92 m,+23% | 低(同一估計器,樣本更少) |
| 跨列時差 × 自車速(不用 scale/平面) | 實測/預測 = **0.77**(平面高估 ~30%) | 較高(四組 0.749–0.791,直接量與分段相加差 1.2%) |
| 早先的空間週期自相關 | ≈ 9.75–10 m(平面正確) | **低,已作廢**(窗內僅 1.2 週期,低於本專案自己「≥2 週期」判準;輸出散布 9.05–10.80) |

**✅ 2026-08-19 更新:用車道線消失點獨立測到上界,20–30% 的警報被推翻。**
平面 y = a·x + b·z + c 的地平線列 = `cy + fy·b`;兩條平行車道線的**消失點必落在地平線上**,
所以量消失點就等於獨立量平面俯仰 —— 不用神經網路、不用速度、不用互相關,
靈敏度 1 影像列 ≈ 0.0375° ≈ 40 m 處 1.2%。車道線用多幀脊線累積抽出(不憑印象填座標)。

hs005(左線 58 點殘差 **1.39 px**、右線 61 點殘差 6.42 px、斜率 −1.37/+1.57 對稱):
| | 地平線列 |
|---|---|
| SHARP 平面隱含 | 595.9 |
| 車道線消失點(實測) | 600.5 |
| 差 | **+4.6 px = 0.171°** |

→ 把整個差值都歸給焦距(k = 1.082),縱向誤差上界是 **20 m 處 ~5%、40 m 處 ~9%**。
**若真有 30% 偏差,消失點會偏約 25 px。它只偏 4.6 px。**
所以下表那兩個 17.6% / 30% 的探測是有偏的(跨列時差法:兩列脈衝寬度差很大——遠列的 13 列帶
覆蓋 2.3 m 路面、近列只有 0.36 m,而週期只有 11.3 幀、脈衝寬 5–7 幀,相鄰週期的重疊會把
互相關峰往小 lag 拉;漆線長估計器則在 k 變 5% 時輸出跳 18%)。**兩者都不再作為證據。**

⚠ 只有 hs005 一支通過抽線品質門檻。dc006 右線只抽到 12 點、殘差 33 px;wow001 右線斜率
+4.29 對左線 −1.43,顯然不是同一車道兩側 —— **這兩支的消失點作廢,沒有納入結論。**
要把上界推廣到其他裝置,得先把抽線做穩(右線常是實線或被遮,比左線難)。

hs005 抽到的車道線像素(**存進案表,補上「像素對從未記錄」的缺口**):
左線 `544,872;548,868;555,864;560,860;564,856;569,852`(x = −1.3723y + 1740.4)
右線 `1318,884;1343,880;1357,876;1349,872;1343,868;1336,864`(x = +1.5690y − 25.7)

**結論是「未解」,不是「已確認 25%」** —— 三個數字 17.6 / 23 / 30 收斂不了。但方向一致,
而且有機制可解釋:`fwd ∝ t ∝ fy`,橫向寬 `∝ fy/fx`,所以**焦距整體錯會放大縱向距離、
幾乎不動橫向寬度**,而 SHARP 正是固定 FOV 假設(fx = 0.7955×寬)。我用「只改射線焦距、
平面係數固定」掃描驗證,漆線長確實隨 k 單調變化,但在 k=0.90–0.95 之間跨過 4 m,
估計器本身的不穩定度蓋過了定位精度。

⚠ **稽核過程我自己犯的三個錯,記著避免重蹈**:
(a) 用「穩定跟車時 |目標絕對速−自車速|」當平面檢驗是**循環的**——`abs = ego + rel×3.6`,
    篩 |rel|<0.5 就把該差值限制在 ±1.8 內,量到 0.10 是建構出來的,不是證據;
(b) 用「近場/遠場 ROI 分別擬合平面比俯仰」也不成立——遠場 ROI inlier 率只有 0.21–0.47,
    那裡根本不是路面(天空/護欄/車輛/門架),RANSAC 擬到的不是路面平面;
(c) 「讓訊號最週期化」的俯仰估計器**有系統性偏誤**——有限窗自相關本來就偏好短週期
    (CLAUDE.md 早記過 ac[2L]/ac[L]≈(W−2L)/(W−L)),它會被拉到 7.6 m,不能用。
另外 hs005 的右車道線像素我一度直接抄了 wow001 的取樣點,那批橫向數字無效——
**再次印證下面那條「車道寬校正的像素對從未記錄」的缺口是真的會害人的。**

**這件事的嚴重性**:若縱向真的偏 20–30%,受影響的是**跟車距離與他車絕對速度**
(自車速不受影響)。而現有的驗證**沒有一項能抓到它**:車道寬錨是橫向的、
跟車自洽檢驗是循環的、1130221 的 +0.5% 是管線 A 的另一台相機。

**要做的**(按順序,2026-08-19 修訂):
1. ❌ ~~跨列時差法有偏誤,不要再用~~ → **這條判決已於同日撤銷**(見上一段的乘性/加性檢定:
   赤字隨間距等比例縮放,不是加性的峰偏移)。跨列時差法就是現行 `odometer_distance_calib.py`
   的核心,是本專案目前唯一直接量到縱向尺度的方法。消失點法仍然有效,但它量的是**俯仰**,
   不是縱向尺度 —— 兩者互補而非互斥。消失點法的瓶頸依舊是**右線抽取**(左線殘差 1.1–1.4 px
   很穩,右線常是實線/被遮而失敗)。
1''. **2026-08-19 實測:三個係數真的能全部由標線解出(hs005,完全不用 SHARP)。**
   代數上有個好性質:某列的車道寬 `W = Δpx·h/(py−y_h)`,**焦距被消掉** → 車道寬直接解掛高。
   - h:由 3.5 m 車道寬,四個列各自解出 1.189/1.190/1.190/1.190 m,**散布 0.0%**
     (SHARP+車道寬錨定得 1.229,差 3%)
   - 地平線列:由車道線消失點實測 600.5
   - f:由 10 m 虛線週期解出中位 **1699 px**,對 SHARP 固定 FOV 假設的 1527.4 差 **+11.3%**
     ⚠ 但 IQR 1143–2617 很寬(漆線起點偵測是弱環),只能當量級參考
   ❌ **當時的方向結論(「SHARP 假設焦距偏小 8–11%、管線 B 低估前向距離」)是錯的,與同日
     稍晚的直接量測相反**(實測是焦距偏**大**、距離**高估** 11–32%)。錯在哪:
     (a) 把消失點與 SHARP 地平線的 4.6 px 差**整個歸給焦距**,但那個差只約束俯仰,
         對 `A = h·f` 沒有約束力 —— 一條方程式解不了兩個未知數,這正是當時自己寫的問題;
     (b) 虛線週期路線的 f 中位 1699 px 的 IQR 是 1143–2617,本來就只能當量級參考,
         不該拿來定方向。**正確的量是 A = h·f 的直接比值(見上一段),不要再引用這裡的 1699。**
   先例:BrnoCompSpeed(本領域標準 benchmark)用的就是消失點標定。

1'. **更根本的選項:讓平面完全不靠 SHARP。** 兩條平行車道線的消失點給俯仰與側傾、
   車道寬 3.5 m 給橫向尺度、虛線週期 10 m 給縱向尺度 —— 三者合起來把平面、scale 與
   有效焦距全部定住,沒有任何學習先驗,對鑑識用途也更好講(全部可稽核)。
   hs005 的左線殘差 1.1 px 顯示條件數是夠的。這同時解掉「一條方程式解兩個未知數」。
2. ✅ **已完成**:縱向錨就是虛線週期(碼表本來就在量),與橫向的車道寬錨聯立即可,
   實作為 `odometer_distance_calib.py`,7 支 demo 有 5 支解得出來。
3. ✅ **已解除**:距離不再是「未經縱向驗證」,而是「已量測、已修正(5 支)」或
   「未通過品質閘、維持未修正並標註(dc003 / dc007)」。自車速自始不受影響。

## ⚠ SHARP 的角色與尚未收緊的一項(2026-08-18 稽核)

有人提出「SHARP 訓練基線只有 60cm,遠場度量不可信」。查證後的正確定位:

**1. 自車速完全不經過 SHARP。** `public_dataset_ego_eval.py` 只 import
`pick_points / dash_cycle_speeds / extract_odometer_signals`;碼表函式內沒有任何
plane / ply / scale 參照。→ comma2k19(2.18)、KITTI 邊界、夜間修復、15 案迴歸
**全部與 SHARP 無關**,不受此議題影響。

**2. SHARP 的逐像素深度從不進入量測。** `pixel_to_plane` 是解析射線×平面交點
(`t = c/(dy−a·dx−b)`),SHARP 只貢獻三個平面係數 (a,b,c),且由**近場 ROI** 擬合;
絕對尺度本來就不信 SHARP(各機 0.88~2.9),由法定標線錨定。也就是說「度量深度不可信」
這個前提**本架構已經假設了**。

**3. 真正的暴露面是平面傾角 a,b** —— 純量 scale 吸收不了它。敏感度(掛高 1.23m):

| 俯仰誤差 | 10m | 20m | 40m | 60m |
|---|---|---|---|---|
| 0.10° | 1.4% | 2.8% | 5.4% | 7.8% |
| 0.25° | 3.4% | 6.6% | **12.4%** | 17.5% |
| 0.50° | 6.6% | 12.4% | 22.1% | 29.9% |

**4. 現有證據能界定到什麼程度**:
- 七支 demo 的平面殘差只有深度的 0.21~0.42%;四個有記錄的掛高**精確重現**
  (dc006 1.317/記錄1.320、dc007 1.279/1.260、hs005 1.230/1.229、wow001 1.220/1.219)。
- ⚠ 但**掛高對俯仰很鈍**:車道寬是橫向量的,幾乎不受傾角影響 → 它錨定 scale,卻沒約束傾角。
- 直接檢驗(把已知的 10m 虛線週期透過平面在 8~40m 各距離量出來,hs005):
  全體中位 **9.75m(−2.5%)**、分窗 9.40/10.00/9.05/10.80、**無單調趨勢**。
  ⚠ 但窗長只裝得下約 1.2 個週期,低於本專案自己「窗內≥2 週期」的判準 → **檢定力不足**。
- 結論:傾角誤差大致 ≲0.3°(對應 40m 處 ≲15%),**但沒有收得更緊**。

**5. 兩個要修的缺口**:
- 🔴 **車道寬校正的像素對從未記錄**(CLAUDE.md 只記了 scale/CV/掛高)→ 那些 CV
  (hs005 0.66%、wow001 1.52%、dc006 2.0%、dc007 7.9%、dc003 11%)**無法重現**,
  而「同一條線在不同影像列解出的 scale 是否一致」正是**唯一直接測俯仰的現成檢驗**。
  下次跑校正務必把 `--pairs` 存進案表。
- 🟡 dc003 由封存 scale 反推掛高 = **0.996m,低於合理區間 1.1~1.5** —— 與該案已知的
  Mio 超廣角問題(CV 11%、遠列高估)一致,其距離數字應視為低信心。

⚠ 稽核過程中我自己犯的兩個錯,一併記著避免重蹈:(a) 用「穩定跟車時 |目標絕對速−自車速|」
當平面檢驗是**循環的**——`abs = ego + rel×3.6`,篩 |rel|<0.5 就等於把該差值限制在 ±1.8 內;
(b) 用「近場 ROI vs 遠場 ROI 分別擬合平面、比俯仰角」宣稱幾何隨距離剪切也**不成立**——
遠場 ROI 的 inlier 率只有 0.21~0.47(近場 0.51~0.94),那裡根本不是路面(天空/護欄/車輛/門架),
RANSAC 擬到的不是路面平面。該對照**不能當作證據**。

## 驗證狀態(截至 2026-07-10)

| 驗證 | 結果 |
|---|---|
| CCTV 海盛 5 案 vs 人工交比法 | 3 案吻合(45.5/48.4/44.6),2 案=失效邊界(遠場、螢幕翻拍摩爾紋) |
| **1130221 前鎮交比案(機車過口)** | 同窗 48.6 vs 人工 48.37 km/h(**+0.5%**);人工幀號=我方−1(輪胎座標對出);全軌擬合 39.6=含減速段 |
| v3 追蹤器 A/B(同幀重跑) | 配對速度差 median≈0(kh012 n=113);「消失後重生」可疑對 8→4;kh013 遠場污染案例 34.1→33.9 保住 |
| 遠場自動拒絕 | 1110822 兩軌 sens 0.83/0.88 自動 far_field ✓ |
| 跨車道尺度 | 4m 線 ~5.3%、50cm 導引線 10–22%(短標線端點誤差大,能用 4m 線就用) |
| 碼表 vs GPS(002 高速) | MAE 2.4 km/h(GPS 燒在 OSD,非循環) |
| 案18(機車)vs 海盛人工 | 三段 −0.8/−1.1/−0.4;猛加速段誠實 no-lock |
| 案78-28(汽車,夜間576p)| seg1 +2.8、seg2 −1.3(落在海盛人工/AI/GPS 離散內);片尾 0.6s 是量測邊界 |
| 案78-29(汽車,夜間1296p)| seg1 +2.1(人工 57.1、車上時速表 ≈60、我們 59.2);seg2 片尾 no-lock |
| dc007(台86 危險駕駛 11s)| 巡航 9 檢查點 MAE≈2.2 vs GPS OSD;急煞 8-9s 誠實 no-lock;紅色目標車全程 id20→105→171 |
| dc006(國1 五股 60s)| 9 有效檢查點 MAE≈2.1 vs GPS(排片頭窗截斷);換道段誠實 no-lock;77 台可測目標車 |
| dc003(國1 三重 60s,Mio 超廣角)| 鎖定段 vs GPS 多在 ±4 內;自車三度換道→覆蓋 ~52%(誠實);60 條軌跡 |
| 偵測器五模型比較(2026-07-11)| v8m 維持預設:qz 真值端到端 −1.6%(11x −4.7%)、dashcam 自洽打平 0.40 km/h;偵測器非瓶頸;夜間 CCTV 未來可考慮 11x(0.70/0.76 vs v8m 0.57/0.58),見 `data/output/model_comparison/` |
| **hs005(海盛 005_白天,國道巡航 18s,2026-08-02)** | **自車速驗證最佳一案**:同窗比對海盛人工畫格法 12 段 **MAE 0.69、bias +0.01、12/12 在 ±3 內**;逐秒 GPS OSD MAE 1.81、18/18 鎖定;四方交叉(座標差分 93.7/OSD 94.17/人工 94.42/我方 94.83)散布 1.1 內;跟車自洽 \|abs−ego\| 中位 **0.10**(593 筆) |
| wow001(WoWtchout 國道三號 118s,2026-08-02) | ❌ 自車速未過:對 OSD GPS MAE 11.97。診斷:週期 12m 已排除;整數 lag 量化 MAE 下限僅 1.97 解釋不了;`_dual_line` 觸發 0 次;直接數脈衝顯示穩定段三點一致 11.0 幀/週期 → **該台 abee OSD GPS 疑高報 5–8%**,另急減速段/訊號劣化段真的鎖錯。兩問題糾纏、無第三方真值可拆。距離與相對速度仍有效 |
| **comma2k19 加州(2026-08-10 首跑,**2026-08-17 診斷更正 + 修好夜間**;24 段/24 分鐘/28,703 幀)** | **第一個公開資料集的管線 B 正式數字**。反推週期 14.37(CAN)/14.52(位姿)vs Caltrans 高速公路 48ft=14.63m(全國 MUTCD 預設 40ft=12.19m 被排除)。端到端(cycle 14.63、全程不用真值),**現行預設(跨時間聯合八度選擇)/ 舊逐幀獨立**:①全 24 段 **7.73** / 10.54;**②排除市區幹道段(含夜間)= 覆蓋 87.6%、MAE 2.79、bias +1.65、p50 1.05、p90 3.37、±3 內 87.9%**(n=22,509)/ 5.32;③再限日間 **1.78**(中位相對誤差 0.98%)/ 1.84;夜間 **4.72**(p90 3.58)/ 12.46。**② 是可引用的端到端數字**(唯一條件=路類為高速公路)。對 CAN 依序 8.34/3.42/2.52;該車 CAN 系統性低報 1.01%。首跑那個 10.54 的「路釘半週期+速度閘控」歸因是錯的,見失效邊界 |
| **comma2k19 跨車輛(2026-08-18;兩台車、33 段、35,809 幀)** | **跨車驗證通過**。資料集全域只有兩台車(`b0c9d2` 在 Chunk 1–2、`99c94d` 在 Chunk 3–10;用 HTTP Range 讀 zip 中央目錄查出,不必下載)。選段規則改成**事前可宣告**的路類條件(整段最低速 ≥ 60 km/h,`tools/comma2k19_select.py`;套回 Chunk_1 把 6 段已知污染全排掉、0 漏網),兩車同規則:**車1 MAE 1.98**(19 段,±3 內 85.6%,半週期 0.2%)、**車2 2.49**(14 段)、**合計 2.18**(±3 內 82.2%、覆蓋 90.4%)。事前選段後車1**夜間反優於日間**(1.69 vs 2.14)。CAN 偏差逐車反向:車1 低報 1.09%、車2 **高報** 0.69% —— 雙車實證「車上儀器不是金標準」 |
| **偵測器換裝受控比較(2026-09-16)** | 換 `yolo11x-seg` 對同一組雷達真值,取 `(影格,雷達物體)` 交集 547 幀:目標速度配對差 **+0.03 km/h**(區塊自助 CI −0.68…+0.78,**含 0**)、近場距離 7.49%→**5.77%**、遠場 −0.01 m(含 0)、接地點 **85% 落在同一像素列**、耗時 ×1.5。→ 偵測器對速度**不是瓶頸**(首次非循環證明),對距離只值 1.7 個百分點。見 `data/output/detector_vs_radar/README.md` |
| **他車距離/速度 vs 原廠雷達(comma2k19,2026-09-01)** | **第一次用儀器驗他車**(先前的 \|abs−ego\| 自洽是循環的)。3 段 908 配對幀:近場(sens≤0.8,≲32 m)距離誤差中位 **7.0%**、目標絕對速 **MAE 3.54**(bias +0.39);遠場 20.5% / 6.53。誤差拆解:自車速項 1.94、幾何項 3.28。3 秒窗平均 **MAE 1.44**(其中平均本身佔一半,另一半是選樣)。無系統性延遲(最佳延遲三段 +1.95/−1.60/+2.00 s,號不一致)。見 `data/output/comma2k19_radar_eval/README.md` |
| **20251029 汽車 8 案批量(2026-07-12)** | 28 段 MAE 1.93;排除 GPS 仲裁判定人工真值可疑的 5 段後 23 段 **MAE 1.37**;±3 內 24/28。三種時基陷阱全現身:33-23 容器謊報(真 28.75)、14-16 VFR(人工 +30%)、44-07 剪掉 60 秒(人工 ÷2.9);GPS OSD 三案仲裁我方較準。見 `dashcam_validation/README.md` + error_distribution.png |

## 重要路徑

- 素材:`data/20260317_AI辨速系統案例/`(海盛案例包:CCTV、汽車/機車行車紀錄器,
  `_manual.docx/xlsx`=人工畫格法真值、`_ai.*`=海盛AI版、`_record.*`=機車案真值;
  **行車紀錄器 OSD 大多燒有 GPS 車速=現成真值**)。20251029 資料夾另有 32 機車+8 汽車鑑定案(檔名含 KPH)。
- 報告/簡報產生器:`tools/report/`(docx + 三份 pptx + 素材裁切腳本 + pptx 結構檢查器;見該資料夾 README)。
- 正式成果:`data/output/report_final/`(CCTV 四台,v3;舊 csv/json 在各 `v2_backup/`)、
  `data/output/cctv_validation/qz1130221/`(交比案驗證)、`data/output/dashcam_demo/`(002/008 +
  2026-07-11 白天多車三支 dc003/dc006/dc007)、
  `data/output/dashcam_validation/`(case18/7828/7829 + 2026-07-12 批量 0135/1005/3323/1416/4316/4407,
  各含 validation_report.md;總覽+時基陷阱三型見該資料夾 README.md)——每資料夾有 README。
- 偵測器受控比較:`data/output/detector_vs_radar/`(11x 評估輸出 + v8m 重跑 + `controlled_comparison.txt`)。
- **現行碼重跑的七支 demo:`data/output/dashcam_demo/*_current/`**(含影片);新舊對照與說明在 `dashcam_demo/_rerun/`。
- 公開資料集:`data/input/kitti/`(raw 2011_10_03_drive_0042)、`data/input/comma2k19/Chunk_1.zip`
  (8.7GB;段落解出到 /tmp,重跑指令見 `data/output/comma2k19_eval/README.md`)、`data/nuscenes/`(v1.0-mini)。
- SHARP 點雲:`data/output/sharp_gaussians/*.ply`;校正:`data/output/lane_dash_calib/<cam>/`。
- TDX 憑證在 `.env`(gitignored);抓 CCTV 用 `tools/fetch_tdx_cctv.py`。
- **幀目錄在 /tmp 會消失**:rec_kh013/rec_hsinchu006/rec_kh012/rec_kh019(可從各
  `report_final/<cam>/original_h264.mp4` 重建,注意 OSD 秒跳在重編碼幀上偵測略異→時間軸
  與舊輸出有 1~3s 局部平移,速度不受影響)、rec_qz1130221(1130221 案 .avi)、
  case*_frames、dc002_frames(002 白天高速 25-45s)、dc008_frames(008 街道 10-30s)、
  c18_frames、c7828_frames、dc003_frames/dc006_frames/dc007_frames(白天三支,全片)、
  c0135/c1005/c3323/c4316/c1416/c4407_frames(20251029 汽車批量;c1416u=pts 均勻 10fps 重取樣、
  c4407a=前 810 幀連續段,重建法見各 validation_report.md)、
  hs005_frames(海盛 005_白天全片 536 幀)、wow001_frames(WoWtchout 國道三號**前 119s** 3570 幀)。
  重建:`ffmpeg -i 原片 -vsync 0 -qscale:v 2 /tmp/X/f%04d.jpg`(002/008 要加 -ss/-t)。

## 各案執行參數(重跑用)

⚠ **2026-08-19 起,行車紀錄器重跑要加 `--distance-correction`**(見上方「距離偏高」段):
hs005 **0.760**、dc002 **0.811**、wow001 **0.815**、dc008 **0.854**、dc006 **0.903**
(dc002/dc008 為 2026-09-16 用反解的 hood-y=900 更正後的值,取代舊的 0.831/0.886);
dc003/dc007 未通過品質閘,維持 1.0 並標註「距離未校正」。
不加旗標(預設 1.0)可完整重現封存輸出。**只縮放前向距離,橫向不動**——
橫向已由車道寬錨定且正確,同時縮放會把它弄壞。
hs005 端到端實測:自車速完全不變(95.05),距離中位 34.16→25.97,
橫向 |lat| 中位 3.84→3.82,靈敏度 0.631→0.480 使遠場拒發減少、有速觀測 1803→1913。

- kh013:ply 64000C000013,scale 2.6802342894315427,cv 0.0742,OSD 時鐘預設 ROI
- hsinchu006:ply CCTV006-ED,scale 1.169174688422128,cv 0.0593,--fps 0.95 --conf 0.25(無 OSD)
- kh012:ply 64000C000012,scale 2.227367377128456,cv 0.1067
- kh019:ply kh019_ref,scale 1.0650914748292453,cv 0.0603
- qz1130221:ply qz1130221_ref,scale 1.0995932795968135(0.5m 導引線,calib ROI "500,340,1920,700"),
  cv 0.10(保守),--fps 10.0533 --min-track-seconds 1.0,label "Qianzhen 1130221"
- dc002:ply dc002_ref,**fps 30.0**,scale 1.01(車道寬 3.5m 錨),**hood 900**(2026-09-16 反解),
  點 "586,800;546,850;672,750",cycle 10(高架用一般 4/6 規格!),--distance-correction 0.811
- dc008:ply dc008_ref,**fps 30.0**,scale 0.884(高度轉移),**hood 900**(2026-09-16 反解),
  點 "1223,800;1272,840;1295,860",cycle 10,--distance-correction 0.854
- case18:fps 30.0,scale 1.05,hood 940,點 "1250,663;1280,799;1410,828;600,805;420,805;850,780"
- case7828:fps 29.773,scale 1.0(未錨定),hood 510,點 "600,420;640,455;680,490"
- case7829:fps 30.0,scale 1.0(未錨定),hood 1060,點 "1219,794;1311,886;1415,989"(2304×1296)
- dc007:ply dc007_ref(f0001),fps 30.0,scale 1.4421(台86 內側車道寬 3.5m 錨,cv 0.079,掛高換算 1.26m ✓),
  hood 1030,點 "1110,900;1216,960;1306,1010",cycle 10(GPS 反驗,台86 高架非 12m!),--octave-threshold 0.95
- dc006:ply dc006_ref(f0900),fps 29.97,scale 0.8663(3.5m 錨,cv 0.020,掛高 1.32m 與 dc008 同機種自洽),
  hood 930,點 "720,700;675,760;1319,820;595,860"(第 4 點=遠列),cycle 10(國1 GPS 反驗!),--octave-threshold 0.95
- dc003:ply dc003_ref(f0900),fps 29.97,scale 0.854(3.5m 近列錨——Mio 超廣角遠列高估,cv 0.11),
  hood 960,點 "1000,700;1180,750;1450,800;704,700;700,750;654,800"(右側前段+左側後段駐留,
  自車 60s 內換 3 種車道;定點前先畫線位軌跡圖挑駐留平台、避開連續縱向亮痕),cycle 10,--octave-threshold 0.95
- case0135:fps 25.0,hood 900,點 "1065,700;1115,760;1195,820"
- case4316:fps **30.5**(OSD 時基),hood 890,點 "1010,720;1105,780;1165,840;1215,880"
- case1005:fps **29.85**(OSD),hood 1000,點 "1030,780;1100,840;1140,900;1185,960"
- case3323:fps **28.75**(OSD!容器 29.97 謊報),hood 960,點 "690,720;615,770;510,830;450,880"(左側虛線)
- case1416:fps **10.0 於 c1416u**(pts 重取樣),hood 540,點 "545,380;505,420;480,470"
- case4407:fps **30.0 於 c4407a**(f1–810 連續段;f811 起是剪掉 60s 後的撞擊段),hood 990,
  點 "1075,700;1165,760;1250,820;1330,880;820,700;880,760;950,820"(主平台+後段左漂雙組)
  (20251029 批量六案 scale 全 1.0 未錨定、cycle 10、ply=<case>_ref)
- hs005(海盛 005_白天):fps **29.8883**,ply hs005_ref(f0270),scale 0.960510(3.5m 錨,cv 0.0066,
  掛高 1.229m ✓),hood 960,點 "779,700;710,750;643,800;572,850"(**左側虛線**,右側是實線無週期訊號),cycle 10
- wow001(WoWtchout 國道三號寶山段):fps 30.0,ply wow001_ref(f2101),scale 0.962990(3.5m 錨,cv 0.0152,
  掛高 1.219m ✓),hood 755,點 "1073,700;1116,720;1152,740;728,700;703,720"(自車在中間車道,左右都是虛線),
  cycle 10。**只抽前 119s**(120s 起是片尾卡片);自車速未過驗證,見該資料夾 README

## 已知失效邊界(報告素材,勿「修掉」——誠實標示是計畫要求)

**🔴 cycle 是「每條路的常數」,不是「每支片的常數」(2026-08-17 由 comma2k19 診斷更正確立)**:
片段若跨越路類,單一 cycle 假設就在跨界處系統性倍速。comma2k19 那批 24 段是用「中位速 ≥ 70 km/h」
篩的,結果放進**起點在有紅綠燈市區幹道、後段才上 I-280** 的段;市區幹道虛線週期約是高速公路的一半
(實測 7.39m vs 14.63m)。半週期率是**位置的階梯函數**:沿共用走廊 lat 37.690 以南 0.0%、
37.710–37.715 為 39.0%、37.715–37.720 為 **90.4%**(那裡畫面直接看得到紅綠燈與短虛線)。
排除幹道段:端到端 MAE 10.54 → 5.32。**碼表量到的是真實週期,錯的是我們餵了錯的常數。**
→ 跨路類的片必須事前依路類切分,否則就得逐段反推週期。台灣案例沒踩到純屬運氣(素材都是整片同一路類)。

**🔴 路類不只兩種,而且速度完全代理不了(2026-08-18,comma2k19 車 2)**:除了「市區幹道 vs
高速公路」,還有**第三種**——高速連接道,實測週期約 **10 m**(既非 14.63 也非 7.32)。
車 2 的 `2018-06-12--00-59-15_7` 全程 **90–96 km/h 定速**穿過它:沿路量到 7.61 → 10.5 → 9.6 → 14.72,
單段 MAE 40.44,把車 2 的整體 MAE 從 2.49 拉到 4.23。**「最低速 ≥ 60」這條選段規則擋不住它**——
速度不只不能代理「幹道 vs 高速公路」,連「連接道 vs 主線」也代理不了。
可用的判別量是**段內週期離散度**`(p90−p10)/p50`:該段 0.70,其餘 32 段全部 ≤ 0.05(差 14 倍),
但**它用到真值,只能事後 characterise,不能當事前選段規則**。
→ 真正可部署的免真值版本是**物理連續性**:1.44 倍的速度階躍在幾幀內不可能。現行 `_join_octaves`
只對八度跳躍收費,1.44 倍不是八度所以漏掉;把轉移項推廣成「罰任何超過物理加速度上限的階躍」
應可自動旗標。**未實作、未迴歸**,見待辦。

**🟡 夜間逆反射標記蓋掉漆線訊號(同上,2026-08-17;⚠ 大部分已修好,見下一段)**:排除幹道後,
剩下的成敗只由**照度**決定——高速公路段日間半週期率 **1.0%**(16 段中 16 段正好 0.0%)、
夜間 **11.9%**。逆反射路釘/貓眼把車燈的光直接反射回相機,夜間比漆線亮得多,亮度訊號的主週期
就變成它們的;日間漆線佔優、標記在量測上等於不存在。用**實測路面灰階**分組(不是時鐘,因為
隧道與無照明高速公路是同一問題):24 段落在 56.0–64.0 與 69.5–76.0 兩群、中間完全空白,
門檻取在那道空隙。夜間**確實有交錯幾何**(同一條 I-280:日間 271 窗全週期/25 半週期,
夜間 37/249),但五個形狀特徵(寬度/峰銳度/面積/振幅/縱向跨列高度)全部分不開 →
**單窗內無解**。台灣國道貓眼是 2× 方向、加州是 ½ 方向,**兩側都只在暗處發作**。
修好後夜間殘留 3.2% 半週期幀、MAE 4.72(日間 1.78),仍是已知的較弱情境。

✅ **修法:基頻選擇改成跨時間聯合決策(`_join_octaves`,2026-08-17)**——每個取樣點跑 Viterbi,
發射項用原本的諧波梳狀分數,轉移項對隱含速度變化收費;候選只取該幀自相關真有的峰(不無中生有),
所以真實加減速自由跟隨、只有「跳八度再跳回」變貴。**轉移項必須尺度不變**:第一版罰絕對 |Δv|
把台灣封存打壞(dc003 覆蓋 57.8→27.1、wow001 中位 89.4→77.9、hs006s 動 229 幀)——
低一個八度的路徑速度變化也低一個八度、永遠更平滑,等於獎勵減半;改罰 **|Δ log v|** 後兩者代價相同。
成效:夜間 MAE 12.46→**4.72**、p90 76.66→**3.58**、覆蓋 82.7%→**90.2%**(八度一致後分裂鎖閘
少丟幀);日間 1.84→1.78。**台灣 15 案迴歸**:`--no-join-octaves` 對改動前模組 9,762 共同幀
**0 幀變動**(重構本身惰性);開啟後 12/15 案零變動、總變動 0.74%,**hs005/hs006s(有人工逐幀真值)
零變動**,dc003 14/15 與 dc006 1/1 朝鄰域共識變好。舊行為用 `--no-join-octaves` 重現。
詳見 `data/output/comma2k19_eval/README.md`。

⚠ **首跑(2026-08-10)寫成「漆線之間鋪路釘造成半週期誤鎖、且是速度閘控」——兩個部分都已證錯**:
(a) 交錯的脈衝列應在寬度/振幅/面積/峰銳度上逐個交替,實測**四個特徵都不交替**,誤鎖窗的交替對比
不比正確窗高、相鄰對一致率 0.53–0.72(擲硬幣 0.50)→ 該幾何不存在,「脈衝寬度匹配濾波判別器」
的前提不成立,**已放棄不必再試**;(b) 速度相依是混淆變項(幹道段與夜間段剛好也是慢的)。
lag 下限 0.25s 仍是真的**可達性**上界(次級週期只在 `v ≤ cycle_sub/lag_min` 內可搜到),但不是成因。
已排除的其他解釋:搜尋帶寬(±120→±60 幾乎沒變)、自動挑點跨線(與 MAE 無相關)、
取樣率(10.4 幀/週期,高於下面那條判準)。

**🔴 碼表取樣率下限(2026-08-03 由 KITTI 實跑發現)**:碼表需要**每個虛線週期 ≳8 幀**,
即 `fps ≳ 8 × v / cycle`。KITTI 10 Hz、85 km/h、週期 11.7m → 只有 **5.0 幀/週期**,
½ 與 2× 的假峰都落在搜尋範圍內,諧波梳狀評分無法分辨 → 反推週期出現三峰(5.9/11.7/23.4m),
整體 MAE 24.7,但**只看鎖對八度的幀 MAE 只有 2.27** → 壞的是八度選擇不是量測鏈。
加大視窗反而更糟(正確群 52%→20%→15%,長窗給倍週期別名更多支持)。
本專案成功案例都在 8.4–15.4 幀/週期(c1416u 8.4、wow001 10.8、hs005 11.3、dc006 12.7、dc007 15.4)。
→ 公開資料集要報管線 B 數字必須用 ≥25 fps 來源;10 Hz 自駕資料集在高速段本質不夠用。

**🟡 遠場距離本身仍偏 20%(2026-09-01 由雷達量到;旗標已於 09-16 補上)**:
遠場距離中位偏 20%、最壞偏 **72%**(comma2k19 seg21:我方 38 m / 雷達 66 m),
近場(sens≤0.8,≲32 m)則只偏 7.0%。三個輸出介面現在都會標示:HUD 遠場只印 `far-range`
不顯示數字、`targets_summary.csv` 遠場速度另立一欄、`ranges.csv` 有 `far_field` 欄(2026-09-16 新增)。
⚠ **偏差本身沒有被修掉**,只是被標示出來;而且**與偵測器無關**——換 `yolo11x-seg` 後遠場
配對差只有 −0.01 m(CI 含 0),兩模型誤差相關 r=0.9986,是幾何靈敏度問題。

**🔴 擦身而過的側向目標,速度不可信(2026-09-01,dc008 id250 實證)**:路邊停放廂型車在
接近過程中量到的距離**先變大**(32.8→34.2 m)才驟降,橫向漂 2.2 m,量到的接近率只有自車速的
**−0.02 倍(應為 1.00)**。原因是**接地點沿車身遷移**(從車尾滑到車側/車頭),是系統性效應不是雜訊。
→ 橫向偏移大且距離近的目標,只報距離、不要報速度。(這也是台灣端「零速錨」不可用的原因。)

近地平線遠場、螢幕翻拍摩爾紋、貼地視角虛線 3D 取樣、碼表對加速段(chirp,解析度~3s)、
超短片(<8s)片尾窗截斷、近停時旁車車流過帶假鎖、LK 光流測速(任何裝置都別信)、
換道期間碼表(掃過非法定標線,線位閘會拒發=誠實 no-lock)、貼身目標車(接地點低於 hood 線,
如 dc007 紅車 8-9.3s 貼右側時無法量)、**急煞交會段的 dashcam 目標 id**(2026-07-16 使用者目視
dc007_trackv3 證實:7.5–11s 急煞俯仰破壞平面假設(藍車 fwd 在 27–33m 跳動)+紅車切入與藍車
影像交會 → id152/157 在兩台車間反覆互換,皆非單一車輛純軌跡;封存版 166/171 同病。
✅ **關聯層已於 2026-08-02 修好**(px 一致性閘,見待辦第 2 項;dc007 紅車全程單一 id);
**縫合層仍會跨車接錯**,見待辦 2c)。

## 待辦/下一步

**使用者最新方向(2026-07-12):完全不做機車(含「量測目標是機車」的案);對現有 demo 的抱怨=
夜間太黑抓不到、白天空路沒車可測;要的是「汽車事故/案件 + 畫面中可量測的他車(對方車)」,
最好是行車紀錄器主人與事故無關的路人視角。**
⚠ 素材盤點結論(2026-07-12):兩個案例包(20251029+海盛)的**所有固定式 CCTV 案交比目標全是機車**
(軸距 1.2–1.3m;1141115 WOWtchout 案也是)→ 依方向全部出局;8 支汽車行車紀錄器案的對方車
都不可測(橫向穿越/被剪掉/行人/視野死角;44-07 撞擊瞬間恰在被剪的 60 秒內)。
**「汽車目標+路人視角」是素材缺口**:候選來源=WOWtchout 類公開事故影片、向海盛要汽車目標案、
白天重錄 TDX。現有最接近的成品=dc007(危險駕駛紅車全程追蹤,路人視角、白天)。

✅ **素材缺口已部分補上(2026-08-02)**,兩支新 demo 見 `dashcam_demo/{hs005,wow001}/`:
- **本機原本就躺著兩筆沒動過的素材**:海盛 005_白天(壓線測試,18s,**還附人工標註真值**)、
  以及 2026-07-13 抓下但從未進 demo 的 `data/input/wowtchout/wow_baoshan_aHXcGNydRsE.mp4`。
  盤點新素材前先查本機。海盛 001/004/009 仍未用(皆夜間)。
- **WoWtchout 頻道擴充法**:`yt-dlp --flat-playlist` 抓 200 支中繼資料 → 依「汽車目標 + 時長≥40s」
  篩 → `curl i.ytimg.com/vi/<id>/hqdefault.jpg` 抓縮圖判白天/路型 → 只下載入選的驗結構。
  5 支下載結果:1 優(wow001)、1 堪用(`wow_d_rvslHBvxE` 岡山市區,被檢舉單截圖切成 5–6 碎段)、
  3 出局(`zSsRp-LihSQ` 後半是**拍電腦螢幕**踩螢幕翻拍邊界、`aGNjgRMqYoc` 藍色塗鴉遮罩蓋住路面、
  `jv9HgCqsJBM` 11s 後全是放大裁切)。**WoWtchout 片尾卡片約佔 24s,選片時長要扣掉。**
- 素材授權:YouTube 公開影片,報告引用須標出處、不可商用。

(2026-07-11:只做汽車、重點是「他車測速」——影片裡要有多台可量測的車。)
✅ 白天多車三支已完成(2026-07-11):dc007(危險駕駛,12 軌)、dc006(77 台可測)、dc003(60 軌)。
各含 range_speed_h264.mp4/ego_speed.csv/ranges.csv/targets_summary.csv/original_clip_h264.mp4。
注意:003/006 實際都是國道(素材夾名「街道」有誤);三支 cycle 全是 10m(GPS 反驗)。
使用者尚未看過三支標注影片觀感。


✅ 20251029 **8 個汽車案全部完成**(2026-07-12):28 段 MAE 1.93(可信真值 23 段 1.37),
含 44-07 時間基準異常釐清(片中剪掉 60 秒;人工距離對、時基錯,×2.899 修正後三方吻合)。
證物影片時基三陷阱(容器謊報/VFR/剪輯跳接)已整理成報告素材,見 dashcam_validation/README.md。

- ~~案 5、43(機車)、32 機車案、其餘固定式 CCTV 案~~ → 2026-07-12 確定不做(機車目標,見上方盤點)
- 取得「汽車目標+路人視角」新素材並跑他車測速 demo(WOWtchout 公開影片/海盛汽車目標案/白天 TDX)
- 白天重錄 TDX(目前素材都是夜間;使用者操作)——1130221 已提供白天路口 CCTV 素材
✅ 簡報 18 頁(2026-07-12)+ 進度報告簡報(2026-08-03 教授看過那份)+ 成果報告書草稿
(2026-07-12,10 圖 2 表)。
✅ **2026-08-17 更新並把產生器搬進版控 `tools/report/`**(原本住 `~/tmp/`,不在版控,
差點被誤判為遺失;素材 17MB 仍在 `~/tmp/pptx_build/assets/`,用 `REPORT_ASSETS` 可覆寫):
- `專題進度報告_20260817.pptx`(14 張,`gen_progress2.js`)——**逐項答覆教授四點**,
  含公開資料集數字、診斷更正、夜間修復、15 案迴歸、三條可計算適用條件、以及需老師協助的兩件事。
- `成果報告草稿_20260817.docx`(`build_report.py`,14 圖 4 表,字數 6.8k→12.9k)——
  新增 3.6 基頻選擇、4.6 comma2k19、4.7 研究誠信更正、4.8 三條適用條件表、4.9 迴歸驗證;
  參考文獻補到 17 筆(BrnoCompSpeed/comma2k19/KITTI/nuScenes/IET 綜述/Caltrans/MUTCD)。
- ⚠ 本機無 LibreOffice 也無 CJK 字型 → **無法轉圖目視**,改用 `tools/report/check_pptx.py`
  檢查越界/圖片變形/文字溢出(那次抓到兩張圖被拉扁,一張差 66%)。**產出仍請在 PowerPoint/Word 開過**。
- **待使用者補:姓名/系所/指導教授/執行期間(封面已留佔位列)、參考文獻編號對齊,並審閱內文。**
- 使用者尚未確認 v3 影片觀感(框跳動/重複計數是否解決);v3 程式碼已 commit(2026-07-13)
## 📌 2026-08-03 教授指示(進度報告後,方向改變)

四點:①連結 object detector 看最準的模型算速度誤差範圍 ②找資料集有沒有連結論文
③全部抓到的準確度算出來、整個資料集平均的速度跟誤差算出來 ④資料集若有 bbox 可微調 detector。
= **改用有論文的公開資料集做外部可檢驗的驗證**。調查與計畫見
`docs/PUBLIC_DATASET_BENCHMARK_PLAN.md`。重點結論:
- **BrnoCompSpeed**(Sochor et al., IEEE T-ITS 2019;20,865 台車、雷射光閘真值、附相機標定)
  是這題目的標準 benchmark,文獻都在上面比 → 對應管線 A,能直接回答第 3 點。
  阻塞:tar 約 200 GB(本機剩 139 GB)、`medusa.fit.vutbr.cz` 此環境連不到 → 需學校網路或向作者要子集。
- ✅ **comma2k19 已下載並跑完(2026-08-10)**:`data/input/comma2k19/Chunk_1.zip`(8.7GB,HF
  `commaai/comma2k19`,MIT)。管線 B 的第 3 點已交付,見驗證表與 `data/output/comma2k19_eval/README.md`。
  新工具三支:`comma2k19_prepare.py`(解壓/解碼/CAN+pose 雙真值 CSV)、
  `auto_odometer_points.py`(**自動挑碼表取樣點**——資料集級評估的前提;hs005 對人工 median|Δ| 0.39)、
  `plot_comma2k19_eval.py`。`public_dataset_ego_eval.py` 加了 `--batch-root/--auto-points/
  --alt-column/--points-json/--dump-dir`;`extract_odometer_signals/dash_cycle_speeds` 加了
  `band_px`(預設 120 = 舊行為,hs005 逐項迴歸相同)。
- ✅ **診斷更正 + 正式數字(2026-08-17)**:第四支工具 `comma2k19_domain_eval.py` —— 把 ECEF 位姿
  轉經緯度、按路類/實測路面照度分層重算,**只讀 `per_frame/*.npz` 與 zip 裡的位姿,不重跑量測**
  (幾分鐘)。可引用的端到端數字是**日間高速公路 MAE 1.84**(不是首跑的 10.54);
  首跑的「路釘半週期」歸因已證錯,兩條正確的失效邊界見上方失效邊界段。
- **本機 nuScenes v1.0-mini 實測不適合當速度 benchmark**:ego_pose 差分自車速中位 18.3 km/h、
  **0% 超過 60 km/h**,全是市區路口/停車場,沒有連續法定虛線 → 碼表無從運作。但其 3D 標註可投影成
  2D bbox,可留給第 4 點微調用。
- 管線 B 自車速的公開替代:**comma2k19**(加州高速公路、CAN 車速,最貼近碼表法)或 KITTI raw(OXTS)。
- 綜述可引用:Fernández Llorca et al., IET ITS 2021(arXiv:2101.06159)——該綜述指出附速度真值的
  公開資料集**只有 BrnoCompSpeed 與 UTFPR 兩個**。
- ⚠ 方法學:我們的尺度錨綁台灣法定標線,到捷克/美國要換規格;虛線週期務必用真值反驗(台灣國道
  實測 10m 非規範 12m)。建議「用資料集自帶標定」與「自行錨定」兩種都跑並比較。

## 🔴 交接狀態(2026-08-19 第二輪收尾,新對話從這裡接)

**優先序(2026-09-16 使用者指示)**:**完整報告不急**;期限不必記錄在此。
使用者當前關心的是「怎麼看到整體改進的效果」。

### 8/19 第二輪(報告輪)做完的事
1. **距離修正係數全案重跑核對**:舊記錄有三組互相矛盾的數字,已用版控參數 + 由
   `original_clip_h264.mp4` 重建影格重跑統一(見上方 🔴 段的表)。**wow001 由拒發變通過**
   (舊的 0.134 散布是加同線分組閘之前的數字)→ 5/7 支可自校。
2. **撤銷 8/18 對跨列時差法的判決**:乘性/加性模型檢定顯示赤字隨間距等比例縮放
   (hs005 跨 5.9 倍、乘性殘差 0.066 m vs 加性 1.300 m),不是「互相關峰被拉偏」。
   同時把 8/19 稍早「焦距偏小、距離低估 8–11%」的方向結論標為錯誤(見 🟡 段)。
3. **報告與簡報已更新到 8/19**:
   - `data/output/report_slides/專題進度報告_20260819.pptx`(21 張,`tools/report/gen_progress3.js`)
     ——8/17 那份的內容全部保留(教授還沒看過),再加跨車輛、第三種路類、SHARP 稽核、
     距離量法與證據、逐機係數、修正效果與前後對照圖;**第 18 頁改成五模型比較表**
     (原本只有條列)。`check_pptx.py` 剩 3 個軟警告(都是多行文字框,實際高度夠)。
   - `data/output/report_draft/成果報告草稿_20260819.docx`(16 圖 8 表,17.9k 字,`build_report.py`)
     ——新增 3.7(縱向尺度自我校正)、4.6 跨車輛表、4.8 第三條邊界、**4.10 距離尺度更正**、
     **4.11 偵測器選型與誤差來源分解**(表 7 五模型×四場景、表 8 三個誤差來源的量級),
     摘要/結論改寫,4.5 加「距離為修正前數值」警語。
     ⚠ 4.11 的數字全部取自 `data/output/model_comparison/README.md`(2026-07-11 那次比較),
     原始 pickle 在 /tmp 已消失,但三支腳本在 `model_comparison/scripts/` 可重跑。
   - 新工具 `tools/report/plot_distance_calib.py`(讀 `_calib/*.json` 重畫證據圖,不必重跑量測)。
   - **仍待使用者補**:封面姓名/系所/指導教授/執行期間、參考文獻編號對齊,並在
     PowerPoint / Word 開過(本機無 CJK 字型,無法轉圖目視)。

### 8/19 第一輪做完的三件事(都已 commit)
1. **comma2k19 跨車輛驗證**:資料集只有兩台車,已補第二台(Chunk_3)。
   兩車 33 段 35,809 幀 **MAE 2.18**;選段規則改成事前可宣告(最低速 ≥ 60,`tools/comma2k19_select.py`)。
2. **SHARP 稽核**:自車速完全不經過 SHARP(已用程式碼確認);但**距離偏高 8–32%**,
   原因是 SHARP 固定 FOV 的焦距假設逐機錯(**平面俯仰大致正確**——但「地平線只差 0.7 px」
   這個說法別再用,擬合出的地平線不穩,俯仰要用車道線消失點當證據)。
3. **距離修正已量出並接進管線**:`tools/odometer_distance_calib.py` 用碼表訊號自己校,
   `dashcam_range_speed.py --distance-correction`(**只縮前向,不動橫向**;預設 1.0 完全重現封存)。
   係數(8/19 全案重跑;dc002/dc008 於 2026-09-16 因 hood-y 反解而更正):hs005 **0.760**、
   dc002 **0.811**、wow001 **0.815**、dc008 **0.854**、dc006 **0.903**;dc003/dc007 未過品質閘
   → 維持 1.0 並標「距離未校正」。**wow001 是加「同線分組閘」後由拒發變通過的**,
   所以是 5/7 支可自校,不是 4/7。對照影片在
   `data/output/dashcam_demo/_calib/*_before_after.mp4`(上=前、下=後,用舊係數算,差 ≤2%)。

### 2026-09-01 這一輪(他車速度的外部驗證)
用 comma2k19 的**原廠雷達**第一次驗證了他車的距離與速度,並結掉「距離修正係數幅度未定案」。
細節見上方 ✅ 段與 `data/output/comma2k19_radar_eval/README.md`。新工具三支已進版控:
`comma2k19_radar_export.py` / `comma2k19_radar_eval.py` / `plot_radar_eval.py`,
外加 **`lane_line_fit.py`**(從累積脊線 RANSAC 擬出左右車道線並印出 `--pairs` 與消失點,
**專門補「車道寬校正的像素對從未記錄」那個缺口**——別再目視挑點);
`odometer_distance_calib.py` 的 1080p 寫死內參已修(對既有係數影響 ≤0.44%,不必改發表數字)。
**成果報告已寫進去**:`data/output/report_draft/成果報告草稿_20260903.docx`
(新增 **4.12 他車量測的外部儀器驗證**——表 9 兩條鏈的獨立性、表 10 分層誤差、
表 11 平均窗與選樣效應分離、表 12 碼表係數對雷達的仲裁、圖 17 證據圖;
摘要/結論/4.5 警語同步更新;17 圖 12 表、約 22.6k 字)。
圖片變形檢查 17 張全部 OK。
**簡報也做好了**:`data/output/report_slides/他車量測驗證_雷達_20260903.pptx`(17 張,
`tools/report/gen_radar_validation.js`)——單一主題,含雷達來源與前處理、**誤差指標定義頁**、
分層結果、誤差分解、時間窗平均、距離係數仲裁、待修正項、**全案驗證狀態一覽表**;`check_pptx.py` 0 警告。
⚠ **使用者對簡報體例的要求(2026-09-03,以後照這個寫)**:①不要以「道歉/之前做錯了」當敘事,
方法學問題就用方法學講法陳述;②每個數字(7%、3.54 km/h)都要先定義是怎麼算的才能出現;
③**少用比喻**,比喻讀起來像 AI 生成;④外部素材(如雷達)的來源與處理步驟要交代清楚;
⑤**「名詞或數值直接講會不知道在幹嘛」**——技術名詞第一次出現處要說它在系統裡負責什麼
(第 2 張:接地點/地面平面/尺度錨/虛線碼表法;第 8 張:中位數/MAE/bias/p90 各自回答什麼問題),
每個結果數字下方要有一條「怎麼解讀」把它換成具體情境(7% → 雷達 20 m 時我方落在 18.6–21.4 m;
3.54 km/h → 實際 100 km/h 會算成 96.5–103.5)。
成果報告 docx 的 4.7/4.10/4.12「研究誠信/自我更正」框架**維持不變**,兩者定位不同。
8/19 的 `專題進度報告_20260819.pptx` 仍是「全案進度」那份,兩者用途不同,都保留。

### 2026-09-16:寄出前的稽核抓到三個事實錯誤,已全部改正
為了把簡報寄給校外研究者,先做了一輪多代理人查核(逐頁對帳數值、比對程式與敘述、
持懷疑立場的審閱)。**確認三個 `wrong` 級錯誤,全部已改**(產生器 + README + docx 三處同步):

| # | 錯在哪 | 原本寫的 | 正確的 | 為什麼會錯 |
|---|---|---|---|---|
| 1 | 素材表的時間欄 | seg21 29.5–59.9 s / seg6 0.1–20.3 / seg10 30.0–53.7 | **4.2–35.9 / 2.1–55.3 / 3.9–57.0 s** | 那是**選段時看的雷達軌視窗**,同一列的距離帶與配對幀卻是**配對後**族群 → 一列混兩個族群 |
| 2 | 雷達串接的成效 | 「切成 142 段(最長 13 幀),串接後最長段 115 幀」 | **切成 204 段(最長 79 幀),串接後還原為單一物體 609 幀(30.4 秒)** | 115 是評估器的 `runs.longest`(**兩側維持同一組配對**的最長區段,相機側也參與決定),不是雷達串接結果;寫混了會讓前處理看起來只救回 19%、且與同頁「30 秒軌跡」自相矛盾。142/13 三段皆不可重現 |
| 3 | 欄位語意的自我驗證 | 「最小相對速 −29.65,同幀 CAN 29.2」 | **894 筆靜止回波的「相對速+CAN」中位 −0.083 m/s = 自車速的 0.29%** | 那組數字**三段都不存在**;而且單一極值本來就是弱論證——三段最小值對同幀 CAN 分別差 5.3% / 0.5% / 1.1%,挑哪一筆會得到不同結論 |
| 4 | 遠場距離的缺陷範圍 | 「距離照樣印在 **HUD 與** ranges.csv」 | **只有 `ranges.csv`**;HUD 與 targets_summary 都正確 | 憑印象寫,沒去讀 render 迴圈(1169–1180 行)。HUD 遠場只印灰色 `far-range`,數字全不顯示 |

→ **教訓(已成標準程序)**:報告裡每一列表格的所有欄位必須**來自同一個族群**;
引用評估器輸出的欄位前要先確認它量的是哪一側(`runs.longest` 是配對統計,不是雷達統計);
自我驗證用**族群統計**不要用單一極值。

**改正後的檔案(舊檔保留,日期即版本)**:
- `data/output/report_slides/他車量測外部驗證_雷達_20260916.pptx`(**20 張,自帶背景可獨立閱讀**,
  給校外讀者用;比 0903 那份多了系統背景、量測原理、素材與參數、雷達資料實例、
  跨國適用性、限制四條與可重現性)。`check_pptx.py` 0 警告。
- `data/output/report_draft/成果報告草稿_20260916.docx`(4.12 的三處數值同步改正)。
- 0903/0904 兩份**內容有誤,不要再寄**。


### 🔴 2026-09-16 使用者決定:**管線 A(路口監視器)不做了**

使用者的原話是「管線A那個路口監視器太差了所以可以別做了」。與 2026-07-12 的素材盤點一致
(兩個案例包的固定式 CCTV 案**交比目標全是機車**,依「不做機車」的方向本來就全部出局)。

**連帶影響,三件事要跟著改**:
1. **教授第 ③ 點因此變成「完整交付」**。原本缺的那一半就是管線 A 的公開資料集數字;
   管線 A 出局後,comma2k19 兩台車 33 段 35,809 幀 **MAE 2.18** 就是完整答案。
2. **BrnoCompSpeed 從待辦移除**(200 GB + medusa 連不到那個阻塞消失了)。
   報告結論章的「後續建議 (1)」還在講它,**要改寫**。
3. 管線 A 的既有成果(qz1130221 +0.5%、海盛 5 案、四台 CCTV 正式輸出)**保留在報告裡當作已完成的工作**,
   只是不再投入新工作。不要把程式碼刪掉。

### 🔴 2026-09-16 發現:「偵測器不是精度瓶頸」的證據已經失效

報告表 8 與結論章都寫「接地點鏈之雜訊約 **0.4 km/h**」,並據此論證投資應放在尺度錨而非換模型。
**那個 0.4 來自 dc006「跟車自洽」測試,而那個測試是循環的**(`abs = ego + rel`,
篩穩定跟車就是篩 |rel| 小 —— CLAUDE.md 自己在三個地方記過這條循環性,但表 8 仍引用了它)。

雷達給出非循環的數字。**同樣取中位數比較**(從 `seg*_pairs.csv` 逐幀重算):

| 來源 | 「接地點 → 距離 → 微分」這條鏈的誤差 |
|---|---|
| dc006 跟車自洽(**循環**) | 0.40 km/h |
| **原廠雷達(非循環)** | **2.75 km/h**(MAE 3.28、p90 6.80) |

**差 6.9 倍。** 我對那 2.75 做過拆解,結論是**目前分不出來是誰造成的**:

| 成分 | 大小 | 依據 |
|---|---|---|
| 尺度誤差 | **~0.06 km/h,可忽略** | 這批目標幾乎同速(相對速度中位僅 0.35 m/s),尺度錯 5% 也只貢獻 0.06 |
| 逐幀高頻抖動 | **σ 0.72 km/h,約佔 18%** | 同一連續配對段內逐幀差分推得 |
| 其餘約 82% | **~2 km/h,來源不明** | 持續數秒的慢漂移;可能是接地點隨視角系統性遷移(**偵測器問題**)、路面起伏、或雷達自身偏差 |

→ 當時的狀態:**「偵測器不是瓶頸」是未證實的主張,不是結論。**

## ✅ 2026-09-16 已由受控實驗結案:偵測器對**速度**不是瓶頸,對**距離**只值 1.7 個百分點

把偵測器換成 `yolo11x-seg`(偵測級分數較高)重跑那三段,對同一組原廠雷達真值比較。
工具 `tools/detector_radar_compare.py`;完整輸出與可重現指令見
`data/output/detector_vs_radar/README.md`。

⚠ **比較必須受控,否則比到的是別的東西。** `comma2k19_radar_eval.py` 對每個雷達物體只留
**一條**我方軌(一對一眾數配對),換模型後可能由**另一個時段**的軌贏得那個眾數 →
兩次評估在為**不同的車、不同的時間**打分。seg21 就是這樣:v8m 打分在 f592–718(7.4–23.5 m)、
11x 在 f734–1053(24.6–44.2 m),端到端 MAE 2.88→11.05,其中**自車速項 3.64→11.49**,
而自車速根本不經過偵測器(兩次執行的 `ego_speed.csv` **逐位元相同**,已驗證)。
→ 正確做法是取 `(影格, 雷達物體)` 的**交集**:同一瞬間、同一台車、同一平面、同一尺度、
同一組碼表讀數,只有偵測器不同。共同配對 **547 幀**。

| 受控比較(n=547) | v8m | 11x | 配對差(**區塊自助 95% CI**,20 幀/塊) |
|---|---|---|---|
| 接地點像素列 | — | — | 中位 **0.00 px**(**85% 的幀完全同一列**),p90 1.8 |
| 目標絕對速度(幾何項) | 3.88 | 3.91 km/h | **+0.03(−0.68…+0.78,含 0)** |
| 跟車距離 近場 ≤32 m | 7.49% | **5.77%** | −0.45 m(−0.68…−0.20,**不含 0**) |
| 跟車距離 遠場 >32 m | 20.16% | 20.15% | −0.01 m(−0.04…+0.01,含 0) |
| 耗時 / 1200 幀 | 約 60 s | 約 90 s | **×1.5** |

**兩次執行的距離誤差相關 r = 0.9986**(幾何項 0.62)——若誤差主要來自偵測器,
兩個結構不同的模型誤差序列應大致獨立;實測幾乎完全重合 → **誤差來自兩者共用的環節**
(尺度、平面、或雷達自身),不是偵測器。

**速度沒改善而距離有改善不矛盾**:改善的是**系統性偏移**,而速度取距離的**時間微分**,
固定偏移在微分中消失。

⚠ **界定要精確,別外推**:
- 近場那組**實質上只有一台車**(178 幀中 171 幀是 seg6 的 radar_obj 1,獨立區塊僅 8 個)。
  naive CI(−0.56…−0.34)過度樂觀,**要引用區塊自助的那個**。
- 這測的是「**換更大的現成同族模型**」,不是「微調」。v8m 與 11x 同為 COCO 預訓練的
  YOLO 分割模型,**85% 的幀接地點落在完全相同的像素列**就是共用標註慣例的直接證據。
  → **排除了「換現成模型」這條便宜路,但沒有排除「針對接地點定義重新標註並微調」**(教授第 ④ 點)。
- 遠場 20% 偏差兩模型完全相同 → **遠場問題與偵測器無關**,是幾何靈敏度問題。
- 基準線自我驗證過:用重建影格重跑 v8m,`ranges.csv`/`ego_speed.csv` 與封存**逐位元相同**,
  評估器輸出完全重現(seg21 geom 2.6630 / seg6 4.4608 / seg10 5.1429)→ 差異全部歸因於偵測器。

### 下一步(新對話從這裡開始;每一項都寫了「在解決什麼」)

**1. ✅ 已完成(2026-09-16):`yolo11x-seg` 對雷達的受控比較**
   - 結果見上方「✅ 2026-09-16 已由受控實驗結案」段:**速度無可測差異**(+0.03 km/h,
     區塊自助 CI 含 0)、**近場距離改善 1.7 個百分點**(7.49%→5.77%)、耗時 ×1.5。
   - **維持 `yolov8m-seg` 為預設**:速度打平、耗時只要三分之二、全部既有驗證建立在它之上。
   - 產物:`data/output/detector_vs_radar/`(11x 的 json/pairs/txt、v8m 重跑 json、
     `controlled_comparison.txt`);新工具 `tools/detector_radar_compare.py`。

**2. ✅ 已完成(2026-09-16):`ranges.csv` 加遠場旗標**
   - `dashcam_range_speed.py` 寫 `ranges.csv` 處新增 `far_field` 欄(0/1),判準與 HUD 相同
     (`not isfinite(sens) or sens > max_sens`);**既有欄位順序與內容完全未動**。
   - ⚠ **封存輸出是加欄之前跑的,沒有這一欄**;要自行判定用 `sens_m_per_px > 0.8`,
     但該欄只印三位小數,恰在門檻上的觀測會印成 `0.800` 而實際值大於 0.8
     (comma2k19 seg21 就有一筆)→ **用印出值重算會有極少數邊界筆不一致**,旗標本身用未捨入值。
   - 說明已補進 `data/output/dashcam_demo/README.md`。

**3. ✅ 已完成(2026-09-16):報告表 8 與結論章**
   - `tools/report/build_report.py`:4.11 節新增**表 8(受控比較)**;原表 8 → **表 9**,
     首列由循環的 0.4 km/h 換成雷達受控數字,並把「對速度」與「對距離」分開陳述;
     後面的 4.12 表號順移(表 9→10、10→11、11→12、12→13,內文引用已同步)。
   - 結論章:量級重要性那段改寫;「兩次自我更正」→ **三次**(加上 4.11);
     後續建議 (0) 遠場旗標改為**已完成**、(1) **管線 A 出局**(BrnoCompSpeed 移出範圍)、
     (3) 微調的目標改為「重新定義接地點標註」而非更大的模型、
     (6) dc003/dc007 改為**不建議強行補校正**(維持邊界標示)。
   - 產出 `data/output/report_draft/成果報告草稿_20260916b.docx`(13 表 17 圖、24.9k 字;
     圖片變形檢查 17/17 OK)。**同日的 `_20260916.docx` 保留不動**(那份可能已寄出)。

**4. 🟡 教授第 ④ 點(nuImages 微調)—— 第 1 項的結果把它的理由改精確了**
   - 第 1 項**只排除了「換更大的現成同族模型」**,沒有排除微調:兩模型 85% 的幀接地點
     落在**完全相同的像素列**,正說明它們共用同一套 COCO 標註慣例。
     → 若要做,目標必須是**重新定義並標註遮罩底緣(接地點)的位置**,不是換更大的模型。
   - 但**預期收益已有上界**:近場距離誤差 7.5% 裡,換模型只動了 1.7 個百分點,
     其餘 5.8% 與偵測器無關(兩模型誤差相關 r=0.9986);**速度則完全沒動**。
   - 🔴 **更硬的理由(2026-09-16 補算):殘餘誤差在近場與遠場需要的接地點修正是「相反符號」,
     所以任何重新標註都修不掉它。** 把距離誤差用 `Δfwd = sens × Δpy` 換算回像素:
     近場(≤32 m,n=443)需要接地點**往下** +1.8~+2.9 px、遠場(>32 m,n=465)需要**往上**
     −5.5~−7.5 px。接地點偏移是**固定像素數**,不可能同時是 +3 與 −6 →
     **殘餘不是標註慣例問題,是幾何參數(A 與 y_h)問題**(與雷達反解 y_h 的結論一致:
     車道線消失點 377.8 / 碼表 374.3 / SHARP 365.2)。
   - 方向性佐證(兩模型分歧的那 15% 幀):11x 的接地點在 **93%** 的情況下比 v8m **更低**
     (平均 +1.86 px),近場距離因此由高估 **+7.1% 降到 +5.1%** —— 換模型買到的正是
     「接地點往下一點點」,而它只夠修近場的一部分。
     → **建議維持不做**,報告中以「收益上界 + 符號矛盾」兩條說明理由。
   - **只有 nuImages 能做**(93,000 張、80 萬物件、有 instance mask)。KITTI/nuScenes/
     UA-DETRAC/BDD100K 主集只有框,微調不到遮罩頭;而且 nuScenes 的框是 3D 立方體投影的
     軸對齊外接框,天生偏鬆,**拿去微調會把框撐大 → 接地點下移 → 距離系統性改變**,是反效果。
   - 已有的準備:`tools/nuscenes_to_yolo_bbox.py`(v1.0-mini 實跑過)。成本:數十 GB + GPU 數小時。

**5. ⚪ 把「超過物理加速度上限的速度階躍」加進 `_join_octaves` 轉移項**
   - **解決什麼**:**一支影片跨越不同路類時,在交界處會系統性倍速**。虛線週期是
     「每條路的常數」不是「每支片的常數」。comma2k19 車 2 有一段以 90–96 km/h 定速穿過
     高速連接道(週期約 10 m,既不是 14.63 也不是 7.32),單段 MAE **40.44**,
     把該車整體 MAE 從 2.49 拉到 4.23。現行邏輯只對八度(2 倍)跳躍收費,1.44 倍漏掉。
   - 這是**目前唯一已知、不需要真值就能部署的解法**。
   - ⚠ 要改量測邏輯 → **必須跑台灣 15 案迴歸**(框架與案表現成:
     `tools/odometer_regression_cases.py`)。台灣案例沒踩到純屬運氣(素材都是整片同一路類)。

**6. ⚪ 使用者親自做**:在 PowerPoint / Word 開過檔,補封面姓名/系所/指導教授/執行期間、
   參考文獻編號。本機無 CJK 字型,只能程式檢查版面,**看不到成品**。

**不要做的**:
- ❌ **dc003 / dc007 的距離校正**。它們現在標示為「未通過品質閘、距離未校正」,
  那是**方法邊界的誠實標示**,正是計畫書要的東西。硬補上去是在掩蓋邊界。
- ❌ **BrnoCompSpeed**(管線 A 已出局)。
- ❌ 拿 KITTI / nuScenes 的框去微調(見第 4 項的理由)。

### 口試講法(已與使用者對過)
主軸:「這次不是新功能,是**把數字變得可信**」。四點進度 → 交付 ②③ → **兩個自我更正** →
為什麼以前抓不到 → 請老師協助兩件事。最該強調的一句:**首跑誤差中位數只有 1.20,
表面很漂亮,問題只在把誤差依位置分群時才顯現** → 「分群檢查」已列為標準程序。
預期問題三題(舊距離能不能用 / 為何不換 SHARP / 第④點為何沒做)的答法見對話紀錄。

### 2026-09-16 第三輪:七支 demo 用現行程式碼重跑(使用者要「看到整體改進的效果」)

封存的 demo 影片是**舊碼**跑的、而且**沒套距離修正**,不代表現在的系統。七支全部重跑到
`dashcam_demo/<案名>_current/`,報告見 `dashcam_demo/_rerun/README.md`,
對照圖 `_rerun/rerun_{ego,range}.png`,並排影片 `_rerun/{hs005,dc008,dc006}_archived_vs_current.mp4`
(上=封存、下=現行)。工具 `tools/report/plot_demo_rerun.py`。

**結論分三層,只有一層看得見**:距離縮小 10~24%(看得見)、自車速實質不變
(七支中六支逐幀中位差 0.00;hs005 對人工真值 0.687→0.708 = 等價)、
可信度類改進(偵測器驗證/遠場旗標/雷達)看不見但決定數字能不能用。
副作用:距離縮小使 `sens` 變好,**可信的近場觀測變多**(hs005 1345→1512、wow001 1956→2406)。

**wow001 是唯一自車速有明顯變化的一支,而且是好的變化**:覆蓋 85.7%→80.6% 看似退步,
但被拒發的 215 幀在封存裡的速度**中位 132 km/h**(133 幀 >110),該片是國道三號、OSD 上限約 100
——拒掉的是假鎖。**歸因已驗證**:同一批影格只加 `--no-join-octaves` 就回到 85.4% 覆蓋、
>110 幀數反升到 299(封存 265、現行 161)→ 差異來自 `_join_octaves` 而非影格重建,
**用 5 個百分點的覆蓋換掉近一半的高速假鎖**。
⚠ 仍有 161 幀 >110 未被拒(t≈95s 的 155 km/h 兩版都在),該片自車速依然未過驗證。

**三道自我檢查**(重跑本身不可信就什麼都不用談):
① 幾何常數 `A = fwd²/sens` 必須**恰好**按係數縮放 → 七支全過(這就是抓到 hood-y 錯誤的方式);
② dc003/dc007 係數 1.0 當對照組 → 距離 24.7→24.8、42.3→41.6 幾乎不動;
③ 五支有記錄 hood-y 的案,重跑平面係數與封存**逐位元相同**。

### 2026-09-16 第二輪:改正後的檔案(舊檔保留,同日第二版加 b)
- `data/output/report_draft/成果報告草稿_20260916b.docx`(13 表 17 圖、24.9k 字;圖片變形 17/17 OK)
- `data/output/report_slides/他車量測外部驗證_雷達_20260916b.pptx`(20 張,`check_pptx.py` 0 警告)
- ⚠ **同日的 `_20260916`(無 b)兩份保留不動**——那是寄給校外讀者的版本;b 版差異只有
  「遠場旗標已修正」的敘述與偵測器受控比較的結論。

### 環境陷阱(會浪費時間的)
- **`/tmp` 的影格目錄每個 session 結束就消失**,要用 `original_clip_h264.mp4` 重建
  (`ffmpeg -i 原片 -vsync 0 -qscale:v 2 /tmp/X/f%05d.jpg`)。
- **跑 `tools/report/gen_*.js` 要 `NODE_PATH=/home/s11244/tmp/pptx_build/node_modules`**
  ——產生器搬進版控了,但 `pptxgenjs` 只裝在 `~/tmp/pptx_build/`(素材也在那,可用 `REPORT_ASSETS` 覆寫)。
- **跑 UniDepth/scipy 要 `export LD_PRELOAD=$CONDA_PREFIX/lib/libstdc++.so.6`**
  (系統 libstdc++ 太舊,缺 CXXABI_1.3.15)。
- ~~dc002/dc008 的 hood-y 從未記錄,由畫面讀出 = 850 / 950~~ → **2026-09-16 已反解出正確值 = 900**
  (兩支皆 EXACT,`tools/solve_archived_hood.py`)。猜錯的代價不是 1.6% 而是 2.4~3.6% 的係數偏差;
  **任何「從未記錄」的執行參數,先去 `tracks.json` 反解,不要目視猜**。
- ⚠ `docs/PLAN_20260819_MARKING_CALIBRATION.md` 的階段 1「標線標定工具」**已被更簡單的做法取代**
  (碼表自己就能校,不必抽車道線、不必右線)。該文件的動機與代數仍有效,工作項已過時。

**下一步優先序(2026-08-17 更新,新對話從這裡接)**:

0. **comma2k19 之後最該做的三件(照順序)**:
   (a) ✅ ~~路釘半週期判別器(脈衝寬度匹配濾波)~~ → **2026-08-17 查證後前提不成立,不做**。
       診斷重做後發現倍速誤鎖是「路類混合」+「夜間逆反射標記」,不是漆線/路釘交錯,
       四個脈衝形狀特徵都不交替。**端到端 MAE 因此從 10.54 修正到 1.84(日間高速公路)——
       是靠把適用條件講清楚拿到的,不是靠新演算法。** 台灣 15 案迴歸因此**不需要**跑
       (程式邏輯完全沒動;只新增了一支純分析工具 `comma2k19_domain_eval.py`)。
       ⚠ 但迴歸框架的案表已重建成 durable 檔案(見碼表迴歸框架段),下次改碼直接可用。
   (a') ✅ **夜間基頻選擇已修好(2026-08-17,`_join_octaves`)**:改成跨時間聯合決策(Viterbi),
       夜間 MAE 12.46→4.72、p90 76.66→3.58、覆蓋 82.7%→90.2%;台灣 15 案 12/15 零變動、
       總變動 0.74%、hs005/hs006s 零變動。細節與踩過的坑(絕對 |Δv| vs |Δ log v|)見管線 B ✅ 段。
       **殘留**:夜間仍有 3.2% 半週期幀、MAE 4.72(日間 1.78)——整段持續鎖在路釘週期時,
       連續性反而幫它站穩。要再進一步得換一個獨立線索(不是脈衝形狀,五個特徵都已否證)。
   (a'') **未做:把修好的碼表回灌台灣封存輸出**。五支 demo 與 20251029 批量的 csv/json
       都是舊碼跑的;dc003/dc006/wow001 有少量幀會變(見上)。要不要重跑正式輸出由使用者決定
       (影片重算成本高,而變動幀數 <1%)。
   (b) **自動挑點在換道段是否過度發話**——dc003 自動版覆蓋 84.4% vs 人工 57.8%,
       人工版當初刻意避開換道段;該片只有 OSD GPS ±4,要仲裁得找有逐幀真值的片(hs005 已做,等價)。
   (c) ✅ **跨車輛已完成(2026-08-18)**:資料集只有兩台車,已補上第二台(Chunk_3),
       合計 MAE 2.18。再抓 chunk 只會增加同一台車的樣本,**不必再擴**。
   (d) **未做:把「超過物理加速度上限的速度階躍」加進 `_join_octaves` 的轉移項**——
       可自動旗標「跨路類」段落(如車 2 那段連接道),是目前唯一已知、可免真值部署的解法。
       要改量測邏輯 → 需跑台灣 15 案迴歸(框架與案表都現成:`tools/odometer_regression_cases.py`
       + scratchpad 的 `rebuild_frames.py` / `odo_regress.py` / `odo_compare.py`)。
1'. 教授四點裡**第 1、4 點(偵測器比較 / 微調)仍未動**;第 2、3 點的管線 B 部分已交付,
    管線 A 仍卡 BrnoCompSpeed 下載(200GB + medusa 連不到)。

(以下為 2026-07-24 起的既有清單)
1. ✅ **碼表八度邏輯重構已完成並 commit(2026-07-24,commit cd4ad55)**:一次性基頻選擇(諧波梳狀評分
   +pulse x-offset 雙線閘),見管線 B ✅ 段。迴歸逐幀 median|Δ|=0.00:五 demo(59.6/100.0/96.3/83.4/55.0)
   **+ 20251029 批量六案已補測**(c0135/c1005/c3323/c4316/c1416u/c4407a,before/after 同幀 git-stash 比對,
   六案 median|Δ|=0.00、覆蓋不變;低速市區案 c1005 37/c1416u 43.5 雙線閘未誤觸發);合成雙倍速 54→108 修為 54.0。
   六案幀重建法見 scratchpad `rebuild_batch.py`(4 案直接抽、c4407a 前 810、c1416u pts 重取樣 302 幀=對上封存)。
2. ✅ **dashcam 關聯 px 一致性閘已完成(2026-08-02)**:關聯除了平面座標,再要求影像空間連續——
   `|Δ框中心| ≤ 0.35×框寬 + 250×dt`。門檻由資料定:正常連續幀的 |Δ中心|/框寬 p99 只有
   0.11–0.18(dc007/dc006/hs005),而 CLAUDE.md 記載的 dc007 跨車跳接 id152/157 是 **0.90/0.91**;
   框寬項讓近車可以在畫面上跑很快(表觀速度隨表觀大小縮放)而不放寬遠車。
   影響 0.36–1.22% 的關聯步驟。驗證:自車速四案**逐幀完全相同**;dc007 紅車不再跳號
   (t=9.6 舊 id152→10.1 舊 id157,新版全程 id164,31.1m 量測值不變);跟車自洽
   dc006 中位 0.40→0.30、dc007 1.30→1.20、hs005 中位持平(p90 1.00→1.20)、wow001 不變。
   代價:dc006 目標 89→80(掉的多是短軌,時長中位 3.9s vs 全體 4.8s,總觀測 −2.8%),
   hs005 22→20 但總觀測完全不變(純重新切分)。
2c. ✅ **縫合層橫移預算閘已完成(2026-08-02)**:`|Δlat| ≤ 1.5×gap + 1.5 m`。
   **「跨空間矛盾」的想法先被否決**——`pixel_to_plane` 是雙射,平面座標與影像座標是同一份
   資訊,不存在互相矛盾。真正的病因是:平面誤差閘比的是「B 起點 vs A 的**等速外插**」,所以
   端點速度估壞的片段可以「外插到別台車身上」還拿到小誤差。wow001 那筆:畫面邊緣車的 lat
   讀值在 8.75/8.36/8.35/8.46/7.70 抖動 → 擬合出 3.6 m/s 的「橫向速度」→ 外插 1.4s 正好落在
   4.8m 外的遊覽車上 → 誤差 1.50m、閘 6.00m,通過。
   **位移不像預測誤差,無法被壞的速度擬合灌水**:車輛換道 3.5m 要 3–5s(0.7–1.2 m/s),
   即使自車與目標反向換道也就 ~2.4 m/s 且撐不過整個 gap。1.5m 常數是接地點噪聲底線——
   真實的同車重接在 2 幀空隙下橫向就差到 1.4m,再緊會剪斷正確接點。
   校準:142 筆被接受的縫合中擋掉 7 筆(4.9%),**29 筆短空隙全保留**,被擋的最小橫移率 2.41 m/s。
   驗證:自車速四案逐幀相同;目標數 dc007 13→13、dc006 80→80、hs005 20→21、wow001 35→35;
   跟車自洽中位全部不變;wow001 的壞接點(25.33→26.73,4.76m)與另一筆 6.81m 都消失。
   稽核工具:`stitch_fragments(..., stitch_log=)` 或環境變數 `DASHCAM_STITCH_LOG=<路徑>`。
2d. **殘留:關聯層(gap ≤ 0.8s,active window 內)仍有超額橫移接點** —— dc006 35 筆、
   dc007 3、hs005 3、wow001 1,橫移率 4.3–7.7 m/s。這些通過了 px 一致性閘(遠處小目標時
   5m 橫移只有幾十 px),要擋得在關聯也加橫移預算;但遠場 lat 本來就雜訊大(現行縫合閘就
   拒絕跨遠場端點),貿然加會誤殺,需要另一輪校準。
2b. ~~碼表在 100 km/h 級要加次幀內插~~ → **查證後不成立(2026-08-02)**:拋物線次幀內插
   **早已實作**在 `dashcam_range_speed.py` 的 prominence 閘之後(`lag_f = lag + 0.5*(ac[lag-1]
   -ac[lag+1])/denom`,速度用 `lag_f` 不是 `lag`)。只讀 argmax 那一行會誤判成整數 lag。
   高速可靠度已由 hs006s 仲裁澄清(見下方「OSD GPS 不是金標準」),**不需要改碼**。
3. `dashcam_demo/README.md` 重寫:紅車敘述改誠實版(三段+尾段 id 互換警語)、dc008 半速修復、
   近/遠分層數字(89=29+45+15 取代「77 台可測」);使用者確認觀感後把 *_trackv3 轉正
   (五支皆已重跑,見管線 B)。
4. 素材缺口照舊:「汽車目標+路人視角」(WOWtchout 公開影片/海盛汽車目標案/白天 TDX 重錄)。
5. legacy 已清(commit 842abaa,depth_models 8.2GB 等;tools/BEVFormer tar 備份在 scratchpad,
   /tmp 重啟即失);主 README.md 已重寫為兩管線門面。
   保留 sharp_yolo_distance/detect_plate_anchor/refine_anchor_point(現行方法族)。
6. /tmp 幀目錄 WSL 重啟就消失:五支 demo 可從各資料夾 original_clip_h264.mp4 重建
   (指令見「重要路徑」;本次 dc002 600/dc003 1798/dc006 1801/dc007 330/dc008 600 幀已驗證對齊)。
